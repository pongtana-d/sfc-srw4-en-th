Paint a Thai title logo as a COARSE SQUARE-CELL MOSAIC on the grid in image 1. Image 1 is the 2000x640 working canvas with exactly 200 columns and 64 rows; each cell is 10x10 output pixels. Image 2 provides the preferred lettering design. Image 3 provides the ONLY allowed colors.
OUTPUT EXACTLY 2000 x 640. This is a pixel allocation task, not a high-resolution illustration. Decide one palette color per cell. Fill that ENTIRE cell, INCLUDING its grid border, with that one solid color. Remove all visible grid lines in the final result. No detail or color change inside a cell.
Formal contract, origin at top-left:
For every c in 0..199 and r in 0..63, all pixels x=10*c through 10*c+9 and y=10*r through 10*r+9 MUST have exactly the same RGB. In particular the first cell is x0..9,y0..9; next is x10..19,y0..9. Boundaries only at x/y divisible by 10. A bevel 1 game pixel wide must be 10 output pixels wide, never 1 output pixel. A 2-game-pixel stroke is 20 output pixels. A 1-game-pixel staircase goes 10 right/10 down. Think of assembling solid square blocks, 200 by 64, with no seams.
Allowed RGB hex colors: 000000, FFFFFF, FFFFAC, FFFF00, FFDE00, FFBD00, FF9C00, FF7B00, FF5A00, DE3900, BD0000, 391000, 00009C, 00007B, 00005A, 000039. Absolutely NO other colors. No antialiasing, transparency, blur, interpolation, texture, lighting overlays, subcell lines, gradient colors or dithering.
Text exactly "ซูเปอร์โรบอตวอร์ส 4", one line, forward-slanted bold Thai lettering matching image 2. Preserve every Thai mark and legibility. Golden face with intentional flat color bands, continuous pale-yellow bevel ONE CELL thick, orange/red lower face. Dark blue solid extrusion about 8 cells down and 3 right using the four blues. Keep logo including all marks/shadows within logical cells x=4..195,y=3..60. Outside logo pure black cells. Do not draw swatches, labels, menus or scenery.
This will be reduced EXACTLY 10:1: pixel (c,r) of final game sprite is sampled from center (10*c+5,10*r+5) here. Anything smaller than a solid 10x10 cell WILL BE LOST. The desired result is deliberately blocky at this large display size; prioritize this grid over smoothness. Return only the mosaic sprite on pure black.
