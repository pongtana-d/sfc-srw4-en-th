EDIT IMAGE 1, the actual 200x64 game sprite. Image 2 is ONLY a style reference, not the resolution target. Image 3 gives the only permitted RGB colors. Deliver an improved production SPRITE, not concept art.
Highest-priority constraints:
1. The design contains EXACTLY 200 columns by 64 rows of logical pixels. Keep the existing composition, Thai characters, marks and numeral placement of image 1.
2. Preferred file dimensions 200x64. If your output resolution cannot be this small, deliver exactly 1600x512 with EACH logical pixel represented as one perfectly solid 8x8 square. All boundaries aligned to multiples of 8 from canvas origin. Absolutely no details inside any 8x8 block. Never render a high-detail logo and then simulate pixelation. Think and draw at 200x64 first.
3. EXACT palette ONLY: RGB hex #000000 #FFFFFF #FFFFAC #FFFF00 #FFDE00 #FFBD00 #FF9C00 #FF7B00 #FF5A00 #DE3900 #BD0000 #391000 #00009C #00007B #00005A #000039. No intermediate colors, opacity, blended edges, texture, noise, dithering, glows, gradients between palette entries, color management tint or anti-aliasing. Solid #000000 background, no transparency.
4. Literal Thai text "ซูเปอร์โรบอตวอร์ส 4". Preserve every Thai vowel and mark exactly as in image 1; no extra text.
Refinement at LOGICAL pixel scale: connected pale-yellow/white bevel only one pixel thick; smooth intentional staircase contours; coherent gold flat bands using palette steps; preserve at least 1-2 pixels in letter counters; blue continuous extrusion about 8 logical pixels downward and 3 right, with 4 flat depth shades; clean separation between adjacent letters. No stray bright pixels. Maintain at least two logical pixels margin around all marks and shadows.
The previous attempt failed because it returned 2216x709 and over 135000 colors. Do NOT repeat that. This asset must look IDENTICAL before and after direct integer downsampling. Fidelity to the 200x64 grid and exact 16 colors is more important than decorative detail. Return only the sprite.
