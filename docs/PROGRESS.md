# PROGRESS — SRW4 Thai/English Text & UI Rebuild

สถานะรวม: **IN PROGRESS / ยังไม่ใช่ release build**
วันที่อัปเดตล่าสุด: 2026-08-30
แผนหลัก: PLAN.md

ไฟล์นี้เป็น tracker เดียวของงาน ห้ามใช้ข้อความในเอกสารเก่าหรือชื่อไฟล์ build
ตัดสินสถานะปัจจุบันแทนไฟล์นี้

---

## 1. วิธีใช้ tracker

### สถานะ

| สถานะ | ความหมาย |
|---|---|
| TODO | ยังไม่เริ่ม |
| IN PROGRESS | กำลังทำและมีงานค้าง |
| PARTIAL | มี implementation/evidence บางส่วน แต่ยังไม่ผ่าน target ใหม่ |
| RECHECK | ของเดิมมีผลแล้ว แต่ต้องตรวจใหม่เพราะ architecture/target เปลี่ยน |
| BLOCKED | ทำต่อไม่ได้จนกว่าจะได้ evidence หรือแก้ dependency |
| DONE | ผ่าน gate ของ phase พร้อมหลักฐาน |
| REJECTED | วิธีเดิมไม่ใช้ต่อ ต้องบันทึกเหตุผล |
| CLOSED | blocker ถูกแก้หรือย้ายไปเป็นงานติดตามแล้ว |

### กฎการเปลี่ยนสถานะ

- เปลี่ยนสถานะพร้อมเพิ่ม evidence ในตารางหรือ session log
- DONE ต้องมี command/build/ROM/state/artifact ที่ตรวจซ้ำได้
- ภาพจาก save state ที่ไม่มี genuine redraw นับเป็น evidence ไม่ได้
- ถ้าข้อสรุปขัดกับของเดิม ให้ใช้ REJECTED กับวิธีเดิม ไม่ลบประวัติการตัดสินใจ
- ทุก phase ต้องผ่าน gate ใน PLAN.md ก่อนเริ่ม phase ถัดไป
- code/config ที่เปลี่ยนต้องอัปเดต tracker ใน commit เดียวกัน

---

## 2. Snapshot ปัจจุบัน

### สิ่งที่เสร็จในรอบปัจจุบัน

- P0–P6 ปิดแล้ว: corpus/token contract, new glyph atlas, VWF/window,
  runtime core และ variable catalog pools มี build/runtime evidence
- production build เป็น current-source ROM เดียว SHA-256
  `374189c25adbfeab321861e0bf91924811b5ea225b4b76d5da08a4b2f3c56956`
- command menu ใช้คำไทยเต็ม โดยป้าย `Spirit` ล็อกเป็น `สปิริต`; static overlay
  contract ผ่านแล้ว แต่ canonical Mesen gate ต้อง rerun กับ build hash ปัจจุบัน

### สิ่งที่ยังไม่เสร็จ

- P7: Attack branch ยังไม่มี native map-idle state ที่ศัตรูอยู่ในระยะ
- P8/P9: naming keyboard/typed buffer/save-load, intermission, option,
  part effects และ field routes ยังต้อง genuine redraw แบบ same-build
- P10: release regression, final report, rollback commit และ docs freeze

---

## 3. Phase tracker

| ID | Phase | สถานะ | หลักฐานปัจจุบัน | งานถัดไป | Gate |
|---|---|---|---|---|---|
| P0 | Freeze, cleanup, baseline | DONE | PLAN/PROGRESS ใหม่, stale links และ whitespace ตรวจแล้ว; `tools/build.py --check` ผ่านแบบ byte-identical | เริ่ม P1 evidence package ของ en command menu | เอกสารหลักไม่ขัดกันและย้อนกลับได้ |
| P1 | Reverse en active path | DONE | E-101 ถึง E-107: active parser, source streams, direct glyph path, tilemap, frame/cursor และ DMA ของ en command menu มี artifact ทำซ้ำได้ | เริ่ม P2 census/contract จาก corpus และ runtime controls | model ของ en menu ไม่อ้าง pool `$D2:8613` |
| P2 | Token/memory contract | DONE (WRAM runtime recheck in P5/P9) | E-201 ถึง E-205: corpus/runtime inventory, byte-band contract, encode/decode และ ROM budget ผ่าน | ใช้ contract นี้เป็น input P3/P4; re-probe WRAM ก่อน runtime hook | encode/decode และ ROM budget ผ่าน |
| P3 | New glyph atlas | DONE | E-301: atlas 651/651, proof sheet, metrics และ allocation ผ่าน | P4 ใช้ manifest/metrics นี้สร้าง dynamic window fixtures | glyph coverage 100%, proof sheet ผ่าน |
| P4 | Reference VWF/window | DONE | E-401 ถึง E-403: corpus sweep zero overflow, command frame geometry ตรง en, fixture English/ไทย/mixed/ชื่อยาวผ่าน atlas จริง | ใช้ window contract เดียวนี้เป็น input P5/P7 | pixel golden/overflow ผ่าน |
| P5 | Runtime core/fixture | DONE (performance recheck in P9) | E-501 ถึง E-506 และ E-158/E-164: ordinary/battle adapters, guard, dynamic border, arena sweep และ genuine redraw ผ่าน; production battle gate replay ได้ 6/6 | เก็บ timing/VRAM/DMA/VBlank เป็น full-route gate ใน P9 | pixel/tilemap/guard ผ่านทุก context; performance รอตรวจทั้งเกม |
| P6 | Variable pools/catalogs | DONE | E-165/E-171/E-172: story 9,382 records, unified stock-run contract และ current weapon/unit/pilot catalogs ถูกสร้างจาก clean ROM พร้อม pointer/control round-trip | รักษา allocation/slot assertions ระหว่าง integration | ทุก id ที่ใช้จริง encode/repoint ได้และไม่มี allocation overlap |
| P7 | Pilot command menu | IN PROGRESS | E-145 ถึง E-150, E-173 และ E-197: frame/parser/selection ผ่าน; labels ไทย `เคลื่อนที่/โจมตี/พลังจิต/สถานะ` ผ่าน canonical reentry และ Spirit branch โดย battle ไม่แตก | หา native state ที่มี Attack ในระยะและตรวจ Attack ก่อนปิด phase | command ทุก branch รวม Attack genuine redraw ผ่าน |
| P8 | Surface integration | IN PROGRESS | E-178 ถอน snapshot production dependencyแล้ว; E-188/E-190 ปิด title/naming labels/presets; inventory 36 surfaces, unknown 0, runtime verified 18 และ build verified 7 | ปิด Attack, naming keyboard/typed buffer, field/intermission/option/part runtime routes | ไม่มี pending_runtime/runtime_partial |
| P9 | Full regression/performance | IN PROGRESS | E-180–E-182/E-193–E-196: canonical battle/menu/status, isolated cold boot, WRAM isolation, DMA ceiling และ VBlank/forced-blank timing ผ่าน | naming type/delete/save-load และ remaining surface routes/timing | zero clipping/hang/residue และทุก gate same-build |
| P10 | Cutover/docs freeze | TODO | ยังไม่มี release report | ทำ final build, hash, rollback, docs | rebuild ได้ deterministic |

---

## 4. Feature tracker

| Feature | สถานะ | Definition of done เฉพาะ feature |
|---|---|---|
| English new glyph | BUILD VERIFIED / RUNTIME PARTIAL | E-301 coverage ครบ; runtime-proven surfaces ผ่าน atlas ใหม่ แต่ remaining runtime routes ยังต้องตรวจ |
| Thai glyph | BUILD VERIFIED | E-301 atlas 651/651 พร้อม proof/metrics/allocation |
| Thai precompose | BUILD VERIFIED | corpus clusters precompose และ missing glyph เป็น build failure |
| VWF | RUNTIME PARTIAL | reference sweep และ ordinary/battle/menu surfaces ผ่าน; remaining surfaces ยังรอ runtime route |
| advance zero | DONE | fallback mark ไม่กินความกว้าง; precomposed cluster advance ครั้งเดียวตาม locked metrics contract |
| token format | DONE | E-201–E-205 และ E-171 ยืนยัน direct/extended/control round-trip และ global stock IDs |
| string pool | DONE / RUNTIME PARTIAL | story/catalog variable records และ pointers deterministic; remaining screens รอ runtime verification |
| runtime name | PARTIAL / RECHECK | ชื่อผู้เล่นผ่าน new glyph/VWF และไม่ทำลาย naming/save buffer |
| window spec | BUILD VERIFIED / RUNTIME PARTIAL | schema/build assertions ครบ; remaining surfaces รอ redraw |
| tile frame | RUNTIME PARTIAL | command/status/main frames ผ่าน genuine redraw; remaining screens รอ route |
| command cursor | RUNTIME PARTIAL | open/down/up-wrap/reentry/Spirit ผ่าน; Attack branch ยังขาด |
| ordinary engine | RUNTIME VERIFIED | story/runtime-name routes และ reentry ผ่าน genuine redraw |
| battle engine | RUNTIME VERIFIED | battle quote lifecycle และ map return ผ่าน canonical 6-frame gate |
| naming screen | RUNTIME PARTIAL | labels/preset/confirm ผ่าน isolated cold boot; keyboard, typed buffer และ save/load ยังรอ gate |
| surface inventory | PARTIAL | 36 surfaces, unknown 0; ยังมี pending_runtime 5 และ runtime_partial 2 |
| overflow audit | BUILD VERIFIED | corpus sweep/build assertions ปฏิเสธ width/bytes/glyph ที่ผิด contract |
| emulator regression | TODO | cold boot, redraw, navigation, reentry, save/reload ครบ |
| deterministic build | BUILD VERIFIED | production SHA `1e21408f…cdf25b7` สร้างซ้ำจาก clean ROM; final release recheck อยู่ P10 |

---

## 5. Evidence log

ใช้รูปแบบนี้เพิ่มหลักฐานใหม่:

    E-XXX
    วันที่:
    phase/feature:
    input:
    command หรือวิธีทดสอบ:
    ROM/build/commit:
    state และ genuine redraw:
    ผล:
    artifact/report:
    คำตัดสิน:
    ข้อจำกัด:

### Evidence ที่มีแล้ว

| ID | วันที่ | เรื่อง | ผล | คำตัดสิน |
|---|---|---|---|---|
| E-001 | 2026-08-24 | ภาพ en command menu | เห็นกรอบกว้างและคำเต็ม Move/Attack/Spirit/Status | ยืนยันว่าต้องขยาย frame/tilemap จริง |
| E-002 | 2026-08-24 | clean เทียบ en ที่ $D2:8613–$D2:865D | source block เดิมเหมือนกัน | block นี้ใช้แทน active en menu ไม่ได้ ต้อง trace path ใหม่ |
| E-003 | ก่อนแผนนี้ | existing precomposed atlas | baseline ประมาณ 625 token / bitmap ราว 10 KB | ใช้เป็น asset baseline แต่ต้อง re-audit English/runtime |
| E-004 | ก่อนแผนนี้ | existing Python/ASM pipeline | มี atlas, reference, blitter, catalog/repack และ tests | reuse ได้เฉพาะส่วนที่ผ่าน target ใหม่ |
| E-005 | ก่อนแผนนี้ | ROM layout | clean 3 MB, expanded target 4 MB | ใช้ 4 MB เป็น capacity baseline |
| E-006 | 2026-08-24 | P0 baseline | `.venv/bin/python tools/build.py --check` สร้าง ROM 4 MB ซ้ำได้ SHA-256 `d7d3bcbf91489222fce979b894ae8afafeebe6b8f6ca15b76a901823c6072029` | ปิด P0 |
| E-007 | 2026-08-24 | dialogue + runtime name fixture | ROM `--relocate thai --names --check` ผ่าน deterministic; Mesen sweep record `11_E973` โดยใส่ `$FB:1E80` = `เจส` เทียบ reference 51 cells ต่าง 0 | ยืนยัน vertical slice เฉพาะบทสนทนา; หน้าตั้งชื่อยังไม่ผ่าน |
| E-008 | 2026-08-24 | naming-screen live trace | state `Dai-4-ji Super Robot Taisen (Japan) (Rev 1)_1.mss` วาดผ่าน `$C1:84E4`, source pointer `$1A-$1C`; พบ fixed ranges `$CC:AB5E–ABE3`, `$CC:AC53–AC8A` และ runtime-name commands `$CC:ACC2/$ACC5/$ACCA` | naming ต้อง route parser/classifier/width ไม่ใช่ดัก rasterizer อย่างเดียว |
| E-009 | 2026-08-24 | Thai preset runtime | `--naming-presets --check` deterministic; Mesen redraw ของ state จริง โดยใส่ `เจส` ในทุก runtime-name buffer แสดง glyph ไทยและหน้าจอ input ปกติ | preset pool และ runtime-name path ใช้ glyph ไทยได้; ยังไม่ทำ keyboard ไทย |
| E-010 | 2026-08-24 | dialogue runtime-name regression | tests 192 ผ่าน; Mesen sweep `11_E973` โดยใส่ `$FB:1E80` = `เจส` เทียบ reference 51 cells ต่าง 0 | naming preset adapter ไม่กระทบบทสนทนา |
| E-101 | 2026-08-25 | en reference byte/control-flow census | `tools/analyze_reference_rom.py` ยืนยัน en SHA-256 `7cac…661f2c`; เปลี่ยน stock 343,314 bytes/9,119 runs; ordinary และ battle raster calls ไป `$F0:E045`; command-pool เก่า `$D2:8613–865D` ยังไม่ใช่ active source | ต้อง trace source path ตอน command menu สด ห้ามใช้ pool เดิมเป็น target |
| E-102 | 2026-08-25 | en reproducible route ถึง map | `tools/lua/p1-command-route.lua` สร้าง `build/p1-en/map.mss` และภาพ genuine redraw ของ map reference ได้ | state ยังอยู่ scripted setup; ยังไม่ใช่หลักฐาน command-menu geometry |
| E-103 | 2026-08-25 | en live command label source | `build/p1-en/command-probe.txt` trace `$F0:E045` จาก `$1A-$1C` ผ่าน stream `FE:D85C` Move, `FE:D864` Attack, `FE:D892` Spirit, `FE:D8C6` Status; `tools/analyze_p1_command.py` dump byte stream ครบ | byte แรกของแต่ละ label ไม่เข้า `$F0:E045`; ห้าม hook renderer นี้เป็น owner ของ string ทั้งบรรทัด |
| E-104 | 2026-08-25 | en live frame/tilemap | ก่อน/หลัง `build/p1-en/command-probe.*.tilemap.bin` ต่าง 80 cells เป็น component เดียวใน BG `$7E:A000`: x=13..20, y=11..20, 8×10 tiles (`104,88`, `64×80` px); JSON reproducible อยู่ `build/p1-en/command-menu-evidence.json` | target frame ไม่ใช่ legacy 3-cell; pilot ต้องมี builder ของตัวเอง |
| E-105 | 2026-08-25 | en redraw DMA | trace `$420B` หลัง A พบ channel 1: `$7E:DD00`, 512 bytes ไป CGRAM `$2122` ตอน redraw และ steady state `$7E:DF00`, length 0; ไม่พบ glyph VRAM upload ใหม่ในช่วง capture | menu reuse tile/glyph VRAM ที่มีอยู่; ต้องวัด budget ของ core เราเองใน P5 |
| E-106 | 2026-08-25 | en geometry/cursor | screenshot genuine `command-probe.png` และ `command-cursor-down-0240/0360.png`: labels 4 rows Move/Attack/Spirit/Status, รายการละ 2 tile rows และ cursor เลื่อนลง exactly 16 px; frame anchor/size ตาม E-104 | contract geometry พร้อมทำ reference fixture |
| E-107 | 2026-08-25 | en parser/raster contract | `build/p1-en/command-menu-evidence.json` เทียบ expected bytes: parser `$C1:83C7–84E4` และ pointer table `$C9:00D8,X/$00:1A–1C` เหมือน clean; ทั้ง `$C1:84E4` และ `$C1:9238` เปลี่ยน `JSL $81:84EB` เป็น `$F0:E045`; raster อ่าน direct bitmap ที่ `$EE:8000` และเขียน arena `$7E:8000` | en เป็น renderer+geometry reference; pilot ไทยต้องมี core/window ของเรา ไม่พึ่ง source format หรือ glyph runtime ของ en |
| E-201 | 2026-08-25 | production glyph census Thai+English | `tools/audit_translations.py --include-ascii` ตรวจ 32 files / 11,943 visible strings (ไม่รวม metadata) ใช้ token 625 ตัว, outside manifest 0, unreadable 0 และ Japanese leftover 0; regression test ครอบคลุม mode นี้ | coverage ของ catalog/script/runtime buffer แยก census ต่อใน P2 |
| E-202 | 2026-08-25 | runtime text/input inventory | `src/srw4/blitter.py` ยืนยัน buffer ชื่อ legacy `$00:1008–1053`; `naming-screen.th.json` มี preset ไทย 25 ชื่อและ label 6 รายการ; control `<NAME:$8000…8033>` มี 18 addresses ใน corpus; digits 0–9 อยู่ใน manifest ครบ | keyboard/name writer เต็มรูปแบบยังเป็น P8; P2 จะไม่อ้างว่ารองรับ input นอก preset แล้ว |
| E-203 | 2026-08-25 | token ownership lock | แก้ `TokenMap`, stream decoder และ reference renderer ให้ใช้ `extended_pages=4` จาก manifest เดียว: direct `$00–CF`, reserved `$D0–EB`, extended `$F0–F3`, `$F4–FF` เป็น engine control; test 74 ผ่านและ compile corpus 9,382 records ผ่าน | `Gap`/`Pad` เป็น layout/padding control ไม่ใช่ visible glyph; ห้ามใส่เป็น `icon:` token โดยไม่มี typed control ใหม่ |
| E-204 | 2026-08-25 | ROM allocation/budget baseline | `tools/build.py --check` deterministic: text stream Thai 449,868 bytes อยู่ใน `$F0–F8` 589,824 bytes (slack 139,956); atlas/table 13,056/65,536; renderer reserve 2,048/65,536; catalog/routing/hook/spare ยังไม่ถูกใช้ | reserve เหล่านี้เป็น baseline ไม่ใช่ proof ของ final surface migration; WRAM candidates ต้อง re-probe P5/P9 |
| E-205 | 2026-08-25 | machine-checkable text contract | เพิ่ม `data/config/text-contract.json`; tokenizer/parser/decoder/reference renderer/catalog อ่าน byte band, terminator และ operand contract เดียวกัน; `tests/test_contract.py` ตรวจ direct/reserved/engine/extended bands | P2 token/control contract lock แล้ว; ยังต้อง consolidate WRAM candidate map ก่อนปิด P2 |
| E-301 | 2026-08-25 | P3 atlas re-audit | `tools/build_atlas.py` สร้าง 651/651 glyph, 650 bitmap/10,400 bytes, failure 0, advance 3–8 px; proof sheet `build/atlas/proof-sheet.png` ตรวจแล้ว; source เป็น composed 563/drawn 75/icon 11/stock-import 2 (space, ♥ only at build time) | atlas พร้อมเป็น production input; stock runtime renderer ยังห้ามใช้ |
| E-401 | 2026-08-25 | P4 corpus width sweep | `tools/render_lines.py --sweep` render 9,382 messages / 16,753 lines: widest 240 px, overflow 0, too tall 0, unrenderable 0; แก้ profile records `48_7436`, `48_74E7`, `48_4912`, `48_736E` ที่เกิน 1–6 px | ต้องทำ window builder ให้ใช้ width policy นี้แทน fixed/legacy frame |
| E-402 | 2026-08-25 | command menu reference window | เพิ่ม window-specs.json และ srw4.window.menu_layout; test reproduce en border tilemap 2011/2019/2012, anchor (104,88), frame 8×10, label anchor (112,96), cursor x=104 และ row step 16 px; labels English วัดจริง 22/32/29/32 px และ label ไทย 75 px ขยายเป็น 14 tiles | builder พร้อมเป็น contract ให้ P5/P7; ต้องทำ runtime tile allocator |
| E-501 | 2026-08-25 | P5 65816 fixture regression | แก้ blitter extended range ให้รับ 4 pages จาก text contract และเพิ่ม fixture `$F4/$F5`; tools/check_blitter.py บน Mesen ได้ 18/18 pixel/tile/pen/dirty match reference | line core ใช้ได้; ยังไม่มี runtime frame/tilemap/cursor allocator |
| E-403 | 2026-08-25 | P4 command-menu fixture coverage | `tests/test_window.py` วัด label จาก production atlas แล้วสร้าง frame จาก spec เดียวสำหรับ English, ไทย, mixed และชื่อยาว; ทุก label overflow 0, frame สูง 10 tiles, cursor/label count ตรงรายการ และ frame ไม่ต่ำกว่า 8 tiles | ปิด P4 reference/window gate; P7 ยังต้องเชื่อมกับ menu จริง |
| E-502 | 2026-08-25 | P5 context guard on hardware | fixture ROM ล้อม dialogue context `$7E:C5C0–C8DF` ด้วย canary 16 bytes ก่อน/หลังทุก stream; `tools/check_blitter.py` บน Mesen ตรวจ 18/18 match และ `context guard ok` | line core ไม่เขียนเกิน reservation; tilemap/window bridge และ arena/DMA budget ยังเหลือ |
| E-503 | 2026-08-25 | P5 dynamic frame bridge on hardware | เพิ่ม `draw_window_frame`: รับ tilemap cursor/outer width/height จาก context และเขียนเฉพาะ border ลง shadow `$7E:A000`; fixture เทียบ 32×32 words กับ `menu_layout()` จาก P4 บน Mesen ได้ 1024/1024 ตรงกัน ทั้ง en command frame 8×10 ที่ (13,11) และ Thai long-label frame 14×4 ที่ (13,1) | dynamic border contract ใช้ได้; content glyph-tile allocation, cursor state และ game DMA/arena ยังต้องเชื่อม |
| E-504 | 2026-08-25 | P5 real dialogue arena regression | build ไทย (`--relocate thai --names`) deterministic; `tools/check_dialogue.py` ได้ record `03_3B08`, 49 cells ต่าง 0; `tools/sweep_dialogue.py --per-block 1` ได้ 46/47 records จาก 47 blocks, 0 mismatch (record `30_D263` ย้าย cursor จึงรายงาน `repositions` และไม่ถูกนับผ่าน); runtime-name record `11_E973` โดยยึด `$FB:1E80 = เจส` ได้ 51 cells ต่าง 0 | core/arena ผ่าน evidence หลาย block; ยังต้องตรวจ visible DMA, navigation/reentry และ surface menu จริง |
| E-505 | 2026-08-25 | P5 visible dialogue DMA proof | `tools/check_dialogue.py` ให้ Lua capture `build/reports/dialogue.png` หลัง record `03_3B08` วาดเสร็จ; ภาพแสดงบทสนทนาไทยใน window บน map จริง และ arena เทียบ reference 49 cells ต่าง 0; checker ลบ artifact เดิมก่อน run และ fail หากไม่มี dump หรือ screenshot ใหม่ | ยืนยัน line core ผ่าน DMA ไปจอจริงใน dialogue; reentry/navigation และ command-menu surface ยังเหลือ |
| E-506 | 2026-08-25 | P5 dialogue reentry | `tools/check_dialogue.py --skip 1` ได้ record `03_3B41`, 23 cells ต่าง 0; `--skip 2` ได้ record `03_3B5D`, 17 cells ต่าง 0 และ screenshot `dialogue-reentry-2.png` แสดงข้อความไทยใน window ต่อเนื่องหลัง message ก่อนหน้า | dialogue core ไม่ค้าง state ข้ามอย่างน้อยสอง reentry; P7 command-menu lifecycle และ surface navigation อื่นยังเหลือ |
| E-108 | 2026-08-25 | P7 clean command-menu source/lifecycle | capture clean command menuจาก map stateที่เข้ากันได้แสดงกรอบญี่ปุ่น 5×10; stock raster `$81:84EB` รับ source `$D2:8615…`; static table ยืนยัน master slot 13 ที่ `$C9:00FF` ชี้ `$D2:8103`, และ slot labels เริ่ม `$D2:8613`; en เปลี่ยน descriptor slot เดียวเป็น `$FE:D2DD` (370 slots, command อยู่ slots 112–126) ขณะ parser เดิมคงอยู่; arena `$7F:8000` เปลี่ยน 140 bytes ขณะเปิด menu | repoint descriptor เป็นแนวทางถูกต้อง แต่ห้าม hook raster/repoint อย่างเดียว: clean lifecycle ยังเขียน border/content 5×10 จึงต้อง trace จุดเปิดที่ owns frame, tilemap และ cursor ก่อน |
| E-109 | 2026-08-25 | P7 parser-caller census | `exec-watch.lua` ในช่วงกด A เปิด clean menu: `$82:843B` และ `$83:89F5` เรียก `JSL $81:83C6` อย่างละ 1 ครั้ง (parser รวม 2); parser เขียน frame-entry ที่ `$81:844B` 20 ครั้ง และ text tile entries `$81:84A8/$84BD/$84D4` รวม 12 ครั้ง ก่อน raster `$81:84E4` 8 ครั้ง | lifecycle มีอย่างน้อยสอง caller; ยังต้องระบุ contract/ordering ราย callerก่อน hook เปลี่ยน frame |
| E-110 | 2026-08-25 | P7 command-menu owner | `p1-command-probe.lua` ผูก caller กับ raster บน clean map redraw: `$82:843B` และ `$83:89F5` hit อย่างละ 1 แต่ raster ทั้ง 8 ครั้ง (`$D2:8615/17/1A/1C/33/35/51/53`) เกิดหลัง `$82:843B` เท่านั้น; static flow ของ entry นี้คือ `A=$AC`, `JSL $81:83C6`, แล้ว `JML [$F5EC]` | `$82:843B` คือ hook boundary เฉพาะ command-menu ที่รับผิดชอบทั้ง frame/labels; `$83:89F5` ไม่ใช่ renderer owner |
| E-111 | 2026-08-25 | P7 menu-context WRAM | `wram-probe.lua` บน clean genuine command redraw (load map state, frame 170 reset, A frame 180, dump frame 360) ตรวจ context `$7E:CC00–CF1F` ได้ 0 write; context อื่นที่จองไว้ก็ 0 | menu context ที่ประกาศใช้เป็น renderer state ได้ แต่ต้องตรวจซ้ำหลัง hook จริง |
| E-112 | 2026-08-25 | P6 catalog migration coverage | เพิ่ม `catalog_pool.compile_pool()` ซึ่งบังคับ pointer ครบทุก slot, duplicate record, terminator และไม่ข้าม bank; `tools/audit_catalog_pool.py` รับ `slots` เฉพาะ source ที่ประกาศ pointer table `$D2:8103` (ไม่ปน scenario/terrain catalog อื่น) และ resolve Spirit IDs 1–30, special-skill IDs 32–63, blank aliases และ MONO/STEREO/none ที่มี stock padding ได้ 370/370 slot | source coverage gate ผ่านเท่านั้น; ยังต้อง compile payload, repoint descriptor 13/16 และตรวจ ROM runtime ก่อนถือว่าย้าย catalog แล้ว |
| E-113 | 2026-08-25 | P6 catalog-13 offline payload | `catalog13.build()` tokenise catalog mode ครบ 370 slot เป็น 222 variable records; `tools/build_catalog13.py` สร้าง `$FA:0000–0A65` (end-exclusive) ขนาด 2,661 bytes พร้อม report bytes/width/overflow ต่อ slot (overflow 0) | payload deterministic และ command labels เป็นคำเต็มแล้ว; ยัง uninstalled จน adapter+descriptor repoint ผ่าน hardware |
| E-114 | 2026-08-25 | P7 selection-band contract | OAM ก่อน/หลังเปิด command menu clean/en ตรงกัน 544 bytes; กด Down ใน en แล้ว BG tilemap สลับ attribute ทั้ง 6×2 cells ของแถวเดิม `$2500…250B → $2100…210B` และแถวใหม่กลับกัน. clean callsite `$83:89F5`/parser เพิ่ม 1 ครั้ง แต่ raster ไม่เพิ่ม | cursor เป็น selection palette band ใน tilemap ไม่ใช่ sprite; `$82:843B` ต้องสร้าง surface และ `$83:89F5` ต้อง mirror band ต่อทุก navigation |
| E-115 | 2026-08-25 | P7 catalog migration boundary | Mesen จาก map state ยืนยัน wrapper `$82:843B` ต้อง preserve input `A=$00AC`; การล้าง A ทำให้ parser ไป `$D2:6AC0` แทน command labels. แก้แล้ว parser อ่าน `$FA:04FF…`; parser route ต้องไม่ `PLA` ซ้ำเมื่อเจอ control มิฉะนั้น return stack พังและจอดำ. ทดลอง ordinary renderer โดยไม่ hook lifecycle แล้วไม่จอดำ แต่ UI ที่ redraw ร่วมกันยังเพี้ยน | catalog 13 ใช้ร่วมหลาย surface และต้อง dispatch context/line-state ต่อ surface; ห้ามถือ `--command-menu` เป็น build ทดสอบจนมี ordinary catalog adapter/context dispatcher ครบ |
| E-116 | 2026-08-26 | P7 scoped lifecycle experiment | ROM `build/srw4-th-command-p7.sfc` hook `$82:843B → $FB:0C60`, `$83:89F5 → $FB:0CA6`, raster `$C1:84E4 → $FB:0BD3`; Mesen trace ยืนยัน command glyph 24 ครั้งจาก `$FA:0500…05B9` เข้ารaster dispatcher และไม่มีจอดำเมื่อเปิด/เลื่อน/ปิด. เมื่อ scope special state เฉพาะระหว่าง parser call ภาพสุดท้ายไม่มี menu (แม้ parser ครบ) | lifecycle/line-base contract ของ `$82:843B` ยังไม่สมบูรณ์; ห้ามปล่อย `--command-menu`. ต้อง trace tilemap writes และ state หลัง parser return ก่อนแก้ frame ต่อ |
| E-117 | 2026-08-26 | P7 shadow/DMA probe | 10 frames หลังเปิด menu ทั้ง clean/P7 มี DMA `$7E:DD00`, len `$0200` ที่ frame 182; write counter clean: `A000` 50 bytes/98 writes, `DD00` 62/68; P7 adapter ที่เขียน `$A000`: 110/422 และ `DD00` ยังคง 62/68. ทดลองให้ adapter เขียน `$DD00` เพิ่มเป็น 84/112 แต่ภาพ menu ยังไม่ขึ้น | DMA source เพียงอย่างเดียวไม่บอก tilemap geometry/destination. หลักฐาน en เดิมชี้ frame ที่ `$A000`; ห้ามเปลี่ยน base หรือ stride จน trace copy/PPU destination ครบ |
| E-118 | 2026-08-26 | P7 callsite register contract | wrapper `$82:843B` preserve A, X, Y และ DB รอบ `menu_surface` ก่อนเรียก stock parser; deterministic build `8c4a2378…` และ Mesen ไม่จอดำ แต่ frame ยังไม่ปรากฏที่ frame 190 ขณะที่ parser `$FA:0500…05B9` ครบ | ไม่ใช่ X/Y/DB clobber เพียงอย่างเดียว. ต้อง trace `$F5EC` continuation และ menu-state writes ของ callsite เทียบ clean |
| E-119 | 2026-08-26 | P7 parser-pointer preservation | clean end-state หลัง command parser คือ `$CB=$000000`, แต่ adapter เดิมรั่ว `$CB=$FA0000`. เปลี่ยน `menu_raster_dispatch` ให้ push/pop `$CB-$CD` บน stack แทน menu scratch; build `15ff63a2…` กลับมา `$CB=$000000` และถึง `$80:F612` เช่น clean | pointer lifecycle ถูกต้องแล้ว แต่ screenshot frame 190 ยังไม่มี menu; ให้ focus `$D0`/line-base/tile arena contract ต่อ |
| E-120 | 2026-08-26 | P8 Intro page 2 persistent redraw | state กลางจาก `sstate1` ที่ frame 4600 รันต่อเนื่องกับ ROM `--intro` เดียวกัน: `intro-overlay-page2-0900.png` และ `intro-page2-continuous-1500.png` เป็นไทยทั้งสองจุดระหว่าง scroll, ไม่มีจอดำ. การเทียบ state ที่สร้างจาก ROM ไม่มี hook ถูกตัดทิ้งเพราะพก VRAM ญี่ปุ่นมา | หน้า 2 ผ่าน genuine redraw/persistence; full suite 212 passed. ต้องสร้าง fixture แล้วตรวจหน้า 1, 3–5 ต่อ |
| E-121 | 2026-08-26 | P8 Intro page 3 genuine redraw | ขับต่อเนื่องจาก state หน้า 2 ที่สร้างด้วย ROM `--intro` จน `intro-page3-transition-1500.png`; ข้อความเริ่ม `แม้นอยเออ DC ชิงอำนาจอวกาศไป` ตรง `intro-page3.th.json` และไม่มีจอดำ | หน้า 3 ผ่าน genuine redraw; เหลือหน้า 1, 4–5 |
| E-122 | 2026-08-26 | P8 Intro page 4 genuine redraw | `intro-page4-transition-1500.png` จาก chain state ของ ROM `--intro` แสดงข้อความไทยของ `intro-page4.th.json` ระหว่าง scroll และไม่มีจอดำ | หน้า 4 ผ่าน genuine redraw; เหลือหน้า 1 และ 5 |
| E-123 | 2026-08-26 | P8 Intro page 5 genuine redraw | trace จาก state หน้า 4 ยืนยัน `$CC:F10B`; ภาพ `intro-page5-confirm-1260.png` แสดง `ได้ช่วยงานวิจัยของพ่อ ณ สถาบัน` ตรง `intro-page5.th.json` ระหว่าง scroll และไม่มีจอดำ | หน้า 5 ผ่าน genuine redraw; เหลือ fixture redraw ของหน้า 1 |
| E-124 | 2026-08-26 | P7 command-menu context scope | state `srw4-th-naming-presets_1.mss` เปิด command menu; hook `$81:83DA` เลือก pool `$FA` เฉพาะเมื่อ `MENU_ACTIVE=1`. Trace หลังปิด/เปิดใหม่พบ parser ทั้ง `$CC:8Cxx` สำหรับ UI ปกติ และ `$FA:02EE…` เฉพาะ command parser; screenshot ไม่เกิด tile ซ้ำทั่วจอเหมือน descriptor repoint ทั้ง catalog | catalog 13 ต้องไม่ repoint master descriptor แบบ global. P7 เหลือ adapter reflow เพราะ stock vertical cell writer overflow เมื่อ label ไทย/อังกฤษยาว |
| E-125 | 2026-08-26 | P7 stock-writer ordering | disassembly `$81:848E–84E8` ยืนยัน writer คำนวณ/เขียน tilemap ที่ `$84A8/$84BD/$84D4`, commit `$18` ที่ `$84E0`, แล้วจึง `JSL $8184EB` ที่ `$84E4` | raster hook ช้าเกินกว่าจะ reflow layout; overlay ต้อง intercept parser path `$8456` ก่อน stock tilemap writer และข้าม record ที่ render แล้ว |
| E-126 | 2026-08-26 | P7 native command-pointer route | จาก state `srw4-th-naming-presets_1.mss`, route เฉพาะ pointer `$D2:8614…865A` ไป `$FA` ทำให้ label ไทยเข้า path ใหม่ โดย UI/map ส่วนอื่นยังไม่ tile แตกหรือจอดำถาวร; ภาพ `build/repro/p7-native-route-x-0210.png` แสดง frame ใหม่ที่ target แต่ label ยังเขียนทับกรอบเก่า | route/source scope ผ่านระดับต้นทางเท่านั้น; ต้อง hook ก่อน `$81:848E` เพื่อย้าย `$18`/tile base หรือเขียน overlay แล้วข้าม stock writer ก่อนเรียก runtime test ต่อ |
| E-127 | 2026-08-26 | P7 surface redraw/selection rejection | ให้ `menu_surface` redraw หลัง parser และล้าง legacy box ทำให้เหลือ expanded frame เดียวใน `p7-post-surface-0210.png`; แต่ hook `$83:89F5` ที่ redraw ซ้ำทำให้ navigation map แตก (`p7-post-nav-0330.png`). คืน stock selection แล้ว map กลับมา แต่ action/selection label ยังผิด (`p7-nav-stock-select-0270.png`) | REJECTED: ห้ามใช้ `$83:89F5` redraw surface. ต้อง trace selection band และ render only changed row โดยไม่ rerun/l้าง surface ทั้งก้อน |
| E-128 | 2026-08-26 | P7 scoped selection retry | native route จำกัดด้วย `MENU_ACTIVE` เฉพาะ `$82:843B`; `$83:89F5` กลับไป stock parser แล้ว mirror band โดย preserve `X/Y` และใช้ `MENU_WIDTH`. State `build/repro/p7-open.mss` ถูกสร้างหลัง P7 redraw แล้วและเปิดต่อได้โดย map ไม่แตกค้าง (`p7-open-down-0090.png`, `-0450.png`) | harness capture ยังมี frame ดำสลับ จึงยังไม่ผ่าน navigation/reentry gate; ต้องทำ capture ผูกกับ selection event และตรวจ band ที่เปลี่ยนจริง |
| E-129 | 2026-08-26 | P7 raw-D0 mapper rejection | instrumented command raster พบ `$D0` เคลื่อน `0,2,4,…` และ reset ระหว่าง stream; ทดลอง state-machine mapper แล้วภาพ `p7-row-state-0270.png` กลายเป็น glyph ญี่ปุ่นซ้อนแน่นกว่า mapper เดิม | REJECTED: `$D0` ไม่พอเป็น row identity. ต้อง key overlay/reflow จาก native record + tile ownership แทนการอนุมานจาก raster cursor |
| E-130 | 2026-08-26 | P7 native overlay index | `native_index_table()` สร้าง sparse 71-byte map จาก parser pointer `$D2:8614…865A` ไป command index 0–14 (`$FF` สำหรับ non-record); build วางไว้ที่ `$FC` ข้าง overlay payload และ tests ครอบคลุม first/second/gap | lifecycle copier มี key ที่ไม่พึ่ง `$D0` แล้ว; งานถัดไปคือบันทึก index ต่อ row และ copy 1bpp bitmap เป็น 4bpp arena cells |
| E-131 | 2026-08-26 | P7 active-row record list | native router บันทึก index สูงสุดสี่รายการใน menu WRAM; `command-record-probe.lua` จาก `srw4-th-naming-presets_1.mss` หลัง A/genuine redraw ได้ `count=3`, `record=[0,6,12]` ซึ่งตรง Move/Spirit/Status | runtime copier สามารถเลือก bitmap ต่อ row ได้โดยไม่อ่าน `$D0` หรือ token stream แล้ว |
| E-132 | 2026-08-26 | P7 arena-copy rejection | ทดลอง lifecycle copier เขียน overlay 1bpp ลง plane 0 หลัง stock parser แล้ว test-runner ให้ภาพดำทุก capture (`p7-overlay-copy-*.png`) | REJECTED: ยังไม่มี proof ว่า post-parser arena address/color-plane lifetime ใช้ได้. ต้อง trace arena pointer/plane layout ใน owner routine ก่อนเขียน runtime copier ใหม่ |
| E-133 | 2026-08-26 | P1 en command lifecycle | byte diff clean/en พบ en แทน `LDA #$FFFF` ที่ `$82:84BB` ด้วย `JSR $FFF3`; trampoline `$82:FFF3` เรียก `$F0:E000` และคืนค่าเดิม. `$F0:E045` ใช้ `$D0 << 5` เป็น arena offset โดยตรง. P7 ย้าย activation จาก hook `$82:843B/$83:89F5` ไป trampoline `$82:84BB` ที่รัน displaced `LDA #$FFFF / ORA $0E26` ก่อน `JML $82:84C1`; screenshot `p7-en-lifecycle.png` แสดง Thai glyph และไม่จอดำ | ห้าม re-enter `$81:83C6` จาก lifecycle hook อีก. ข้อความเข้าสู่ path ใหม่ได้แล้ว แต่ frame/list ยังไม่มี en window-table contract |
| E-134 | 2026-08-26 | P7 tilemap/arena ordering rejection | ทดลองเรียก `menu_surface` และปล่อย `$D0` ตาม stock ทำให้ `p7-en-surface.png` มี tile กระจายทั่ว map; reset guard เพื่อเรียก frame ที่ post-write boundary ให้ภาพดำ. เทียบ tilemap en/clean ยืนยัน grid target เป็น frame 8×10 ที่ x=13,y=11 และ content tile ids ต่อเนื่องจาก `$0100`; แต่ `$D0` ที่ไม่ map ไป ids เหล่านี้เขียน arena ของ map แทน. คืน experiment แล้ว build `d4520b…` ผ่าน deterministic + 213 tests พร้อม `p7-restored.png` ไม่จอดำ | REJECTED: ยังไม่รู้จังหวะที่ stock จะไม่ overwrite frame/content map. ต้อง trace writer หลัง `$81:84E4`/selection transition; ห้ามสรุปว่า `$7E:A000` เขียนไม่ได้ |
| E-135 | 2026-08-26 | P7 `$82:843B` scope rejection | wrapper ที่เปิด `MENU_ACTIVE` รอบ JSL parser เดิมที่ `$82:843B` ผ่าน deterministic + 213 tests แต่ cold boot ผ่าน native DATA1 ให้หน้า Unit Ability ตีความ label เป็น command glyph (`p7-native-map-scoped-callsite.png`). คืน source/build กลับ hash `d4520b…` แล้ว | `$82:843B` ไม่ใช่ discriminator surface เพียงตัวเดียวใน native flow; ห้ามใช้เป็น scope flag จนกว่าจะมี context owner เพิ่มเติม |
| E-136 | 2026-08-26 | P7 fresh command state / parser timing | state `srw4-th-command-p7_1.mss` ก่อนกด A ให้ trace `$D2:8614…` ก่อน activation แต่ raster หลัง activation. เอา `MENU_ACTIVE` gate ออกจาก native route แล้ว raster เห็น `$FA:0500…0506`, `$FA:0555…055F`, `$FA:05AF…05B9` ครบ; ทดลอง anchor `$D0` ตามสาม record start แล้วภาพไม่เปลี่ยน | pointer route ต้องไม่รอ lifecycle flag; แต่ raster ช้าเกินจะย้าย owner ของ content/frame. ต้อง intercept writer ก่อน `$81:84A8/$84BD/$84D4` พร้อม context ที่แยกจาก Unit Ability |
| E-137 | 2026-08-26 | P7 stock-writer reflow rejection | state `srw4-th-command-p7_1.mss` ยืนยัน writer เข้า `$81:84BD` ที่ cursor `$2298/$231A/$23A0`; ย้ายไป target `$231C/$239C/$241C` ทำให้ Thai path ครบ แต่ screenshot `build/repro/p7-width-reflow.png` เป็นกล่องดำ. tilemap มีเพียง ids `$2100…` ขณะที่ en มี border `$2011…201C` และ pixels owner ใน arena | REJECTED: ย้าย `$18` อย่างเดียวไม่มี arena/frame owner; P7 ต้องสร้าง renderer/copy path ของตัวเองก่อนติดตั้ง reflow |
| E-138 | 2026-08-26 | P7 tile-id/arena contract correction | พบว่า `menu_raster_dispatch` ส่ง shadow-map cursor (`$231C + row*$80`) เข้า `$D0`; แต่ `draw_thai_glyph` ใช้ `$D0<<5` เป็น offset ใน `$7F:8000`. แก้เป็น tile id `$0100 + row*18`, ตรงกับ `menu_refresh_selection` ที่สร้าง 9 cell × 2 tile ต่อ row. build `srw4-th-command-p8.sfc` hash `7b98288b…` ผ่าน 213 tests และ deterministic build | STATIC PASS เท่านั้น: ต้องเปิด state ก่อน A แล้วตรวจภาพ/Down/re-entry จริงก่อนปิด P7 |
| E-139 | 2026-08-27 | P7 overlay range lookup correction | state `srw4-th-command-p8_1.mss` รายงาน tilemap พัง. Audit serializer พบ record เป็น `start,end,width,cells,tile_offset` แต่ raster dispatch เทียบ `$1A` กับ `end` และ `width/cells`; จึงไม่ match record ใดเลย. แก้ range เป็น `start <= $1A < end`; build `srw4-th-command-p9.sfc` hash `c63771b…` ผ่าน 213 tests และ deterministic build | ต้อง visual test ใหม่กับ state ก่อนกด A; P8 ไม่ใช่ candidate แล้ว |
| E-140 | 2026-08-27 | P7 en/patch contract correction | `srw4e.xdelta` สร้าง ROM hash เดียวกับ `en-sample.sfc` (`7cac…`). EN `$F0:E000` ล้าง arena/reset metric และ `$F0:E045` ใช้ `$D0<<5`; ไม่สร้าง custom frame. Audit P9 พบ shared raster โหลด `MENU_ACTIVE` ทับ glyph id ใน A และ custom `$18` reflow/frame แย่ง stock tilemap owner. P10 preserve A, route เฉพาะ bank `$FA`, ถอน reflow และไม่เรียก `menu_surface`; hash `70e3190…`, 214 tests และ deterministic build | P9 ถูกถอนเป็น candidate; P10 ต้อง genuine-redraw test ก่อน แล้วค่อยเพิ่ม EN-style width side effects โดยไม่ยึด tilemap owner |
| E-141 | 2026-08-27 | P7 displaced lifecycle instruction correction | state `savep10.mss` ยัง tilemap พัง. Clean bytes `$C2:84BB = A9 FF 00 1C 26 0E` คือ `LDA #$00FF / TRB $0E26`; trampoline P10 ใช้ `LDA #$FFFF / ORA $0E26` จึงไม่ clear lifecycle bits ตาม stock. เพิ่ม `TRB/TSB` ใน assembler และคืน displaced semantics ตรง instruction; P11 hash `6629548…`, 215 tests, deterministic และ disassembly ยืนยัน `A9 FF 00 / 1C 26 0E / JML $82:84C1` | P10 ถูกถอนเป็น candidate; P11 ต้อง genuine redraw จาก state ก่อน A |
| E-142 | 2026-08-27 | P7 incremental reset / route-only gate | P11 ยัง tilemap พัง จึง reject P8–P11 และกลับมาแยก hook. เพิ่ม `--command-stage route|raster|full`; `route` ติดเฉพาะ native parser route และ shared bank dispatcher โดยส่ง `$FA` ไป stock raster, ไม่ติด command raster/lifecycle/width/frame. `srw4-th-command-route1.sfc` hash `d503ea4…`; deterministic และ test ยืนยัน bytes `$C2:84BB` ยัง stock | ขั้นนี้ตัดสินเฉพาะ “tilemap ไม่พัง”; label ไทย/หน้าตาไม่ใช่เกณฑ์และอาจเป็น glyph ผิดตามคาด |
| E-143 | 2026-08-27 | P7 automated Mesen route/raster isolation | รัน native state เดียวกันเองใน Mesen: clean JP กลับ map ปกติ แต่ route1 แตกทั้งจอ เพราะ parser ส่ง Thai/extended glyph id ให้ stock raster ซึ่งอ่านเกินตาราง จึงยกเลิก route-only เป็น visual gate. สร้าง `srw4-th-command-raster1.sfc` hash `196a6e1…`; Mesen test runner โหลด state, กด A, dump tilemap และจับ `build/repro/raster1-smoke.png` ได้ map ปกติพร้อม command menu โดยไม่แตกทั้งจอ | route+raster เป็น baseline ใหม่; label ยังมั่ว/ซ้อนและ lifecycle/frame ยังไม่เปิด ต้อง trace owner path จาก baseline นี้ |
| E-144 | 2026-08-27 | P7 first-glyph hypothesis rejection | ทดลองให้ native router โหลด glyph แรกจาก destination ใหม่ แล้ว build `raster2`; Mesen เฟรม 120 ให้ PNG hash `4d16e49…` และ tilemap hash `804f13c…` ตรงกับ raster1 ทุก byte | REJECTED และ revert: path ที่วาด state นี้ไม่ผ่านส่วนแก้ดังกล่าว ห้ามเพิ่ม logic นี้โดยไม่มี trace hit |
| E-145 | 2026-08-27 | P7 raster register/page correction | Mesen writer probe ยืนยัน `$FA` 16 glyph เข้า menu blitter แต่ dispatcher ส่ง `A=$05FA` ทุกครั้งเพราะไม่ restore `CODE_SAVE`; แก้ reload A ก่อน JSL แล้วพบ decoder รวม `$F0–$F3` เป็น page เดียว จึงแก้ engine code เป็น `$0100 + page*$0100 + operand`. `raster5` แสดง `MoveSpiritStatus` จาก atlas ใหม่แทน glyph มั่ว | raster path/extended-page contract ผ่าน; ต้องแยก arena line ต่อ record และเชื่อม window owner |
| E-146 | 2026-08-27 | P7 guarded open/frame ownership | native router reset line basesเป็น `$0000/$0012/$0024`; hook `$82:843B` reset record list, เรียก stock parser แล้ววาด surface เฉพาะเมื่อพบ native command records. แก้ legacy clear จากค่าทดลอง x=9,y=7 เป็น staircase ที่ trace จริง x=15..26,y=11..18 และแก้ bottom tile offset `+$0040` เป็น `+$003E`. `full5` hash `7dfca29…` แสดง expanded 11×10 frame พร้อม Move/Spirit/Status และไม่มี stock frame ซ้อน | command set นี้ผ่าน frame/text gate; ยังต้องตรวจ unit ที่มีรายการยาว/ต่างชุด |
| E-147 | 2026-08-27 | P7 navigation/reentry preliminary | Mesen จาก native `_1.mss`: `full5` เปิด/ปิด/reentry ได้ แต่ภายหลังพิสูจน์ว่าสี selection มาจาก stock tile ที่ซ้อนอยู่ ไม่ใช่ expanded surface | SUPERSEDED โดย E-148; ห้ามใช้ full5 เป็นหลักฐาน cursor |
| E-148 | 2026-08-27 | P7 stock selection owner + scoped mirror | Full-WRAM diff บน clean ROM ยืนยัน `$7E:0E3A` เป็น selected-row (`0,1,2,1,0,3`) และ `$0E3B=4` เป็น row count. `full12` เรียก stock parser เดิมที่ `$83:89F5` แล้ว mirror เฉพาะ palette band เมื่อ expanded frame live; ภาพ `full12-selection-sequence-0070…0195.png` ผ่าน Move→Transform→Spirit→Transform→Move→Status wrap. ชุด 3 รายการ `full12-center-reentry-0025…0175.png` ผ่าน Down/Up/wrap, B close และ A reentry | cursor owner ไม่อิง tile scan และไม่ clear/rebuild surface ใน shared caller; ยังต้องตรวจ command branch และ Attack/Wait ก่อนปิด P7 |
| E-149 | 2026-08-27 | P7 command dispatch + Wait surface | `full12-move/transform/status-branch-0140.png` และ `full12-spirit-branch-0155.png` ยืนยัน selection ส่งไป Move grid, Transform, Unit Ability และ Spirit ตามแถวจริง; Spirit กด B กลับ map ได้. หลังเลือก Move แล้วลงช่องปลายทาง `full12-post-move-menu-0210.png` วาด Wait/Transform/Spirit/Status ใน expanded surface โดย Wait เป็น record `$D2:8654` ตาม route contract | branch หลักและ Wait genuine redraw ผ่าน; Attack ยังขาด state ที่มีเป้าหมายในระยะ จึงยังไม่ปิด P7 |
| E-150 | 2026-08-27 | P7 dynamic row count | automated cursor sweep 63 ตำแหน่งพบทหารที่ path `left×3,down×1` มี stock count 5 แต่ router เก็บเพียง 4. ขยาย record capacity จาก hardcode 4 เป็น reserved 8 bytes, frame height เป็น `rows×2+2`, selection loop ใช้ row count จริง. `full13-five-row-open-0075.png` แสดง Move/Transform/Spirit/Ground/Status ครบ และ `full13-five-row-nav-0100…0220.png` ผ่านทั้ง 5 แถวพร้อม wrap | จำนวน item ไม่ถูกตัดที่ 4 แล้ว; Attack genuine redraw ยังขาด state |
| E-151 | 2026-08-27 | battle caller regression | replay End Turn เดียวกัน: clean ROM ผ่าน battle และกลับ map ภายใน 2,400 เฟรม แต่ full build ค้างที่ battle quote `24_768A` (`$F3:DC25`). Trace ยืนยัน battle ใช้ raster state ต่างจาก story: clean `$0E2A=8,10,12…` และ composite `$D0`; adapter VWF เขียน PEN/4 และ two-tile layout. ทดลอง preserve/set `$8000` และ increment metric ทำ checkerboard จึง REJECTED และถอนออก; candidate full18 กลับ hashเดียวกับ clean-menu full13 | P8 ต้องมี battle-surface adapter/fixture แยก; ห้ามอ้าง story-dialogue fixture ว่าครอบคลุม battle |
| E-152 | 2026-08-27 | P7 post-expansion regression | `full18-center-regression-0040/0075/0105/0145.png` ยืนยันเมนู 3 แถวยังเปิด, Down, B close และ A reentry ถูกหลังขยาย capacity เป็น 8 rows; full18 deterministic SHA-256 `3d12bb96…bed9d`, full suite 220 passed | candidate ปลอด checkerboardสำหรับ P7; battle quote ยังเป็น known P8 blocker |
| E-153 | 2026-08-27 | P8 battle EN-contract isolation | disassembly en ยืนยัน battle ไม่ได้เปลี่ยน raster `$C1:9238` จุดเดียว แต่มี width/finalize hooks `$C1:921E/$924A/$92D1` และ allocator/tile helpers อื่น. ทดลอง 4-hook subsetใน full23 ทำให้ HP/EN เปลี่ยน, quote ไทยวาด และ battle กลับ reward/map ได้ ต่างจาก baseline ที่ค้าง; แต่ HUD/sprite/reward tilemap เป็น checkerboard. full21–24 จึง REJECTED และถอนทั้งหมด; full25 กลับ SHA `3d12bb96…bed9d` | lifecycle contract ระบุแล้ว แต่ต้อง port battle tile allocator/reset/finalize set ครบจาก en ก่อนเปิด hook production |
| E-154 | 2026-08-27 | P8 full EN lifecycle probe | mirror ชุด helper EN `$F0:E000–FFFF` ไป bank probe และติด hook battle จริงครบ `$C1:8E62/$921E/$924A/$92D1/$9507/$9776`; ทดลองร่วมกับ generic Thai raster ทั้งแบบ stock metric, floor-8px metric, line clear และ clear-ahead ตาม EN `$E1A4–E1AE` ยังได้ checkerboard/quote ว่าง. ทุก probe ถูก REJECTED และ source คืน full25 byte-identical | lifecycle อย่างเดียวแก้ไม่ได้; battle ต้องมี raster 2×2/metric/state adapter แยก ไม่ reuse story `flush_cells` |
| E-155 | 2026-08-27 | P8 historical renderer recovery | git history มี battle VWF ที่เคยผ่านจริง: state แยก `$7E:FFC0`, rebase `$D2` เมื่อ message reset ย้อน, clear current+2 guards และ width hook ที่ `$C1:9219` (ไม่ใช่ `$921E`). Port บน JP เป็น VWF1–6 วาดไทยได้แต่ยัง checkerboard/ค้าง เพราะฐานเก่าอาศัย lifecycle EN ครบชุด. กู้ pipeline commit `e006590` แล้ว build `srw4-en-th-history-full.sfc` SHA `34272e30…85b0`; replay state ใหม่ผ่าน animation/HP โดยไม่ tile แตก แต่ text ว่างจาก cross-base state จึงยังไม่ release | ใช้ EN-base pipeline เป็น reference ที่ปลอด corruption; ขั้นต่อไปต้องทดสอบ cold/native EN state และย้าย renderer+router ที่พิสูจน์แล้วเข้าฐานปัจจุบัน |
| E-156 | 2026-08-27 | P8 proven cumulative recovery | EN experiment พิสูจน์ pointer chain `master table → dispatch → quote` แต่ generic renderer ค้างทันทีเมื่อวาด Thai. กู้ cumulative stage จาก revision `eddbcada…3142` ซึ่งมี battle renderer/state เฉพาะทาง แล้ว build `srw4-th-proven-full.sfc` SHA `a82a78d8…4995`. Mesen native-state route แสดงบทไทยที่ frame 700/900/1100, เล่น animation ที่ 1300/1600 และกลับ map ที่ 2000/2400 โดยไม่มี tile แตก; cold boot ถึงหน้าตั้งชื่อพร้อม preset ไทยได้. เพิ่ม `tools/build_proven_full.py` และ rebuild byte-identical; current suite 220 passed | ใช้ proven-full เป็น ROM ทดสอบหลักระหว่าง migrate implementation กลับ source ปัจจุบัน; full25 ยังคง safe baseline ของ rewrite แต่ไม่ใช่ P8 candidate |
| E-157 | 2026-08-27 | proven stage promotion | เพิ่ม `tools/build.py --proven-full` ให้คำสั่ง build หลักสร้าง cumulative ROM ที่ผ่าน E-156 โดยตรง, reject การผสม incremental flags, ล็อก revision/SHA และเขียน runtime gates ใน `build/reports/build.json`. เพิ่ม integration test ที่ rebuild จาก clean ROM ใน temp และตรวจ 4 MB/hash/report; full suite 221 passed. Artifact มาตรฐาน `build/srw4-th-test.sfc` byte-identical กับ ROM ที่ replay ผ่าน | ผู้ทดสอบไม่ต้องเลือก full25/EN experiment อีก; เหลือ migration ภายในจาก pinned implementation เข้าชุด module ปัจจุบัน โดย output behavior ต้องคง hash/runtime gate หรือมี evidence ใหม่แทน |
| E-158 | 2026-08-27 | corpus audit after promotion | รัน `audit_translations.py --verbose` กับข้อมูลปัจจุบัน: 32 files, 11,723 strings, 623 tokens; Japanese residue 0, unknown token 0. รายการ empty 61 จุดเป็น metadata/unused slots หลาย catalog และยังต้องแยก policy ก่อนเติม ห้ามนับเป็นบทที่แปลตกโดยอัตโนมัติ | encoding/corpus gate ผ่านสำหรับ ROM ที่ promote; quality review ภาษาไทยยังเป็นงานคนละ gate กับ technical coverage |
| E-159 | 2026-08-27 | automated native battle gate | เพิ่ม `tools/check_proven_runtime.py` ให้ Mesen replay native route และเทียบ SHA-256 ของ 6 golden screenshots: quote frames 700/900/1100, animation 1300/1600, map return 2400. ตัด frame 2000 เพราะอยู่ระหว่าง AI movement; frame 1100 อยู่ตรง text/animation boundary จึงยอมรับเฉพาะสอง adjacent safe golden ที่ตรวจภาพแล้ว ส่วนจุดอื่นต้องตรงค่าเดียว | P8 regression ตรวจอัตโนมัติได้แล้ว ไม่ต้องอาศัยการเปิดภาพด้วยตาอย่างเดียว |
| E-160 | 2026-08-27 | battle contract migration 1 | ย้าย static contract จาก proven revision มาเป็น `data/config/battle-contract.json` + `src/srw4/battle_contract.py`: clean/proven bytes ของ width `$C1:9219`, dispatch `$C1:9238`, stock-FB `$C1:9386`; renderer `$FF:5000` ขนาด 1,796 bytes/hash; WRAM ordinary `$7E:FFA0–FFBF`, battle `$7E:FFC0–FFDF`, scratch `$7E:FFE0–FFFF`. `build_proven_full.py` ตรวจ contract ทุก build และ report อ้างไฟล์นี้; tests เพิ่มเป็น 223 passed | static owner/hook contract อยู่ใน current modules แล้ว; ขั้นถัดไปย้าย generator ของ width/dispatch ก่อน renderer body เพื่อเทียบ byte-identical ทีละ artifact |
| E-161 | 2026-08-27 | battle adapter migration 2 | เพิ่ม current generators `src/srw4/battle_adapters.py` สำหรับ width 81 bytes และ dispatch 23 bytes. Contract ระบุ PC/CPU/dependency/hash ของแต่ละ artifact; tests ประกอบด้วย assembler ปัจจุบันแล้วเทียบ proven ROM byte-for-byte. `build_proven_full.py` สร้าง adapter ทั้งคู่ใหม่ทุก build และหยุดหากต่างจาก artifact ที่รันผ่าน Mesen; cumulative hash ยัง `a82a78d8…4995`, suite 225 passed | width/dispatch ไม่พึ่ง executable source ใน revision เก่าแล้ว; เหลือ renderer body 1,796 bytes, stock-FB adapter และ asset tables ก่อนถอน pinned build bridge |
| E-162 | 2026-08-27 | battle renderer migration 3 | ย้าย source-level assembler/disassembler/renderer เข้า `src/srw4/proven/` และห่อด้วย `src/srw4/battle_renderer.py`; input addresses ทั้ง 15 ค่าอยู่ใน battle contract. Generator ปัจจุบันสร้าง body 1,796 bytes ตรง proven ROM ทุกไบต์ และ `build_proven_full.py` บังคับ regenerate+compare ทุก build | battle renderer ไม่ใช่ binary ฝังและไม่ต้องอ่าน source จาก Git revision แล้ว; source namespace `proven` จะค่อย ๆ ลดเมื่อย้าย API เข้ากับ assembler กลาง |
| E-163 | 2026-08-27 | battle stock/assets migration 4 | ย้าย stock-FB source และสร้าง adapter 113 bytes ตรง proven; ย้าย text/font source package กับ `thai/encoding/icons/shorthand` models มา `data/proven/font`. `battle_assets.py` สร้าง atlas/metrics/mark/shorthand/upper-stack 14 artifacts แล้วเทียบทุก byte ใน ROM. แก้ hidden ROOT dependency ที่ทำ advance ผิดก่อน gate ผ่าน. Build hash คงเดิม, full suite 228 passed และ Mesen golden gate ผ่าน | call chain และ assets ของ P8 อยู่ใน current workspace ครบแล้ว; ขั้นถัดไปให้ current build เป็นผู้วาง artifacts/hooks เหล่านี้เอง แล้วค่อยถอน battle dependency จาก pinned stage |
| E-164 | 2026-08-27 | current-owned P8 placement | เปลี่ยน `build_proven_full.py` จาก compare-only เป็น current placement: หลัง cumulative shell สร้าง ROM แล้ว current `battle_assets`, `battle_renderer`, `battle_stock_fb`, `battle_adapters` เขียน 14 assets + renderer 1,796 bytes + adapters 113/81/23 bytes + hooks 3 จุดเอง จากนั้นค่อยตรวจ final SHA. Report ระบุ `battle_placement.owner = current modules`; final ROM ยัง byte-identical `a82a78d8…4995`, suite 228 passed และ Mesen 6-frame gate ผ่าน | runtime P8 code/assets ไม่ได้ถูกเชื่อถือจาก pinned output อีกต่อไป; pinned stage ยังเป็นเจ้าของ story/catalog relocation และ cumulative UI ซึ่งต้อง migrate แยกก่อนถอน bridge ทั้งหมด |
| E-165 | 2026-08-27 | current story migration | เพิ่ม `story_migration.py` และ `build_current_story.py` ให้ repack corpus ปัจจุบัน 9,382 records ครบ 47 blocks, เขียน master pointers และสร้าง F0–F9 catalog routes ใหม่ใน bank FC. Candidate SHA `2cbbe436…a1610`; replay native Mesen route ผ่าน golden 6 frames ตั้งแต่ quote/animation ถึง map return โดย tile ไม่แตก | story data/routes ไม่ต้องเชื่อ pinned payload แล้ว; candidate ยังแยกจาก stable จนย้าย catalog/UI ที่เหลือและถอน pinned shell |
| E-166 | 2026-08-27 | workspace-owned cumulative builder | ย้าย package cumulative 68 files (629 KB) เข้า `src/srw4/proven_full`, snapshot contract data ที่ `data/proven/full`, ต่อ story compiler เข้ากับ corpus ปัจจุบัน และแยก P8 placement เป็น current helper. `build_current_full.py` สร้างจาก clean ROM โดยไม่ใช้ `git archive`; ล้าง stale tail 41 bytes ที่ post-patch candidate เคยทิ้งไว้. ROM SHA `702f77cb…bac85` ผ่าน Mesen golden 6 frames และถูกโปรโมตให้ `tools/build.py --proven-full` สร้าง `srw4-th-test.sfc` | ถอน runtime dependency ต่อ pinned Git checkout แล้ว; namespace `proven_full` ยังเป็น compatibility API ที่ต้องทยอยแยก owner เข้าชุด module หลัก |
| E-167 | 2026-08-27 | catalog/router owner extraction | สลับ cumulative builder และ UI adapters ทั้งหมดให้ใช้ `srw4.proven.catalogs`, `records`, `relocation`, `catalog_router`, `allocation`, `manifest`, `story`; แก้ manifest ให้ใช้ address helper จาก `srw4.rom`. ลบสำเนา 7 ไฟล์จาก `proven_full`; rebuild ยังคง SHA `702f77cb…bac85` byte-identical กับ ROM ที่ผ่าน Mesen | catalog compiler/record surgery/relocation/route table มี owner นอก compatibility package แล้ว; ถัดไปแยก renderer/stock text และ surface adapters |
| E-168 | 2026-08-27 | renderer/text owner extraction | สลับ ordinary renderer, stock-FB, isolated driver และ text encoding/font/stock/upper-stack pipeline ไป `srw4.proven`; เปลี่ยน imports ของ UI adapters ทั้งหมดและลบสำเนา renderer/text 12 ไฟล์. Build SHA คง `702f77cb…bac85`, full suite 230 ผ่าน | renderer/text core ไม่อยู่ใน compatibility package แล้ว |
| E-169 | 2026-08-27 | compatibility namespace withdrawal | ย้าย naming/menu/status/screens/title/intro adapters เข้า `srw4.proven`, ย้าย coordinator เป็น `src/srw4/cumulative.py`, ROM image contract เป็น `srw4.proven.rom_image`, และ P8 placement เป็น `srw4.battle_placement`. ลบ source ที่เหลือทั้งหมดใน `src/srw4/proven_full`; production build ไม่ import pinned oracle. Rebuild ยังคง SHA `702f77cb…bac85`, tests 230 ผ่าน | ถอน `proven_full` production namespace สำเร็จ; ขั้นถัดไปย้าย snapshot catalog/UI data จาก `data/proven/full` ไป current schemas ทีละ surface |
| E-170 | 2026-08-27 | current intro data route | เพิ่ม `translation_dir` contract ให้ intro builder และให้ cumulative build อ่าน intro 5 หน้าจาก `data/translations` ปัจจุบัน; current `<FD:03>` เทียบ snapshot `<FD:03><A0>` แล้ว overlay ROM byte-identical. ทดลองลบ snapshot intro พบ hidden dependency ว่า stock-run census ยัง discover ทั้ง snapshot translation directory จึงคืนไฟล์และรักษา hash `702f77cb…bac85` | intro translation owner เป็น current data แล้ว แต่ห้ามลบ snapshot translation files จนแยก stock-catalog input manifest ออกจาก directory-wide discovery |
| E-171 | 2026-08-27 | unified stock-run manifest | เพิ่ม `data/proven/stock-runs.json` เป็น explicit union 169 runs ของ runtime-proven UI + current corpus พร้อม input hashes; production ทุก owner ใช้ `StockCatalog.locked()` แทน directory discovery. พบและแก้ contract เดิมที่ stock table/UI ใช้ 168 IDs แต่ story ปัจจุบัน encode ด้วยอีกชุด 155 IDs. Candidate SHA `45e4261e…075b` ผ่าน Mesen battle golden และ command/map smoke; เพิ่ม pointer/control round-trip + dual-corpus coverage tests | stock IDs เป็น global contract ชุดเดียวแล้ว; snapshot filesไม่ควบคุม ID โดยอ้อมอีกต่อไป |
| E-172 | 2026-08-27 | current pilot catalog route | ให้ catalog compiler อ่าน `data/translations` ปัจจุบันโดยตรง; weapons/units เหมือน snapshot ส่วน pilot เปลี่ยนคำตรวจทาน `エレ＝ハンム` จาก “เอลเล ฮุนม์” เป็น “เอเล ฮัมม์”. การเปลี่ยนขนาดทำให้ pointer/pool เลื่อน 2,030 bytes ตามคาด; ROM SHA `0e7b41c2…d7ff` ผ่าน Mesen 6-frame gate, report assertion และ full suite 232 | pilot catalog owner เป็น current data แล้ว; current unit-command schema ยังต้องใช้ P7 expanded/new-glyph adapter ห้ามสลับเข้ากับ legacy 24px writerตรง ๆ |
| E-173 | 2026-08-27 | current P7 command compiler/runtime | ต่อ `p7_cumulative.py` เข้ากับ production build; แก้ command key จาก legacy `$00AC` เป็น measured cumulative `$0022`, แก้ cookie branch กลับด้าน, ให้ router fetch glyph แรกจาก relocated `$FA` record และย้าย selection sync ไป parser-core fallback แทน patch `$0389F5` ที่ทำ battle route พัง. Production SHA `aebbc8e…90d38`; full suite 233 ผ่าน, Mesen battle gate 6/6 และ command gate open/down/up-wrap/reentry/Spirit ผ่าน | P7 current labels `Move/Spirit/Status`, expanded frame และ navigation ปลอดภัยใน production; ยังต้องหา state ที่มี Attack และตรวจ branch ก่อนปิด P7 |
| E-174 | 2026-08-27 | P7 Attack-state census | SUPERSEDED โดย E-185: census รุ่นแรกอ่าน cookie/record list stale จาก savestate และ direction pulse 1 เฟรมอาจถูกข้ามตาม input parity; ข้อสรุป “ไม่พบ Attack” จากจำนวนตำแหน่งจึงใช้เป็นหลักฐานไม่ได้. ส่วนภาพ native route ที่ยืนยัน map menu → END → AI attack ยังใช้ได้ | ห้ามอ้างผล census รุ่นก่อนแก้ probe |
| E-175 | 2026-08-27 | current UI translation ownership batch | แยก `translation_dir` ออกจาก snapshot font root และให้ production map menu/HUD, unit/pilot status, weapon detail, Spirit help, main menu, protagonist settings, option/part effects, series และ terrain effects อ่าน `data/translations` ปัจจุบัน. เพิ่ม payload equivalence tests เทียบ snapshot byte-for-byte; production SHA คง `aebbc8e…90d38`, full suite 235 ผ่าน | ถอน translation dependency จาก snapshot อีก 11 surface โดยไม่เปลี่ยน runtime bytes; screens/intermission และ schema ต่างต้อง migrate ต่อแยกกัน |
| E-176 | 2026-08-27 | cumulative translation cutover | ย้าย naming screen, terrain/scenario relocation, intermission และ screens มา current paths; แยก unit-command compatibility writer ให้ legacy abbreviations ใช้ snapshot อย่างประกาศ แต่ shield ไทยใช้ current และ visible menu owner คือ P7 current overlay. Production SHA ยัง `aebbc8e…90d38`; payload equivalence tests, full suite 235, battle gate 6/6 และ command gate 5/5 ผ่าน | visible translation data ของ cumulative production ไม่พึ่ง snapshot แล้ว; ขั้นถัดไปถอน snapshot font/config/assets owner และทำ inventory gate |
| E-177 | 2026-08-27 | current font/config cutover | census current vs snapshot พิสูจน์ font artifacts 14 ชุด byte-identical และ surface compiler payload ตรงทุก write; สลับ cumulative renderer/catalog/story/intro/UI ให้ใช้ `data/font` และ `data/config` ปัจจุบัน. Rebuild SHA คง `aebbc8e…90d38`. ณ checkpoint นี้ snapshot exceptions เหลือ title asset และ legacy 3-letter command fallback | SUPERSEDED โดย E-178 ซึ่งถอน exceptions ทั้งคู่แล้ว |
| E-178 | 2026-08-27 | zero snapshot production dependency | ย้าย title source ไป `data/assets/title-menu.json`; เพิ่ม `legacy_translation` และ `legacy_visible_width_px` ใน current unit-command schema เพื่อสร้าง fallback MOV/ATK/... จาก source ปัจจุบัน ขณะที่ visible P7 ยังใช้ Move/Attack/...; production `src/tools` ไม่มี reference `proven/full`, report `compatibility_sources=[]`. ROM SHA คง `aebbc8e…90d38`; full suite 235, audit 11,723 strings residue/unknown 0, battle 6/6 และ command 5/5 ผ่าน | snapshot directory เหลือเป็น rollback/evidence เท่านั้น ไม่ใช่ production input; ถัดไปทำ surface inventory/unknown adapter และ Attack/naming gates |

| E-179 | 2026-08-27 | Spirit help runtime verification | replay จาก command-state ที่เปิด Spirit selector แล้วตรวจ genuine redraw 3 แบบ: Down แสดงคำอธิบาย, Right เปลี่ยนคำอธิบายเป็น “หลบเต็ม100% 1ครั้ง”, X เปลี่ยนเป็น “ตรวจดูสถานะเป้าหมาย”; A ปิด selector กลับ map โดยไม่ tile แตก. ภาพหลักฐาน `build/repro/spirit-down/right/x/a.png` ตรวจด้วยตาแล้ว | `menu.spirit_help` ย้ายเป็น runtime_verified; ยังไม่เรียก automated gate เพราะ alias state ต้นทางไม่เหลือใน workspace ห้ามอ้างว่า rerun ได้จนสร้าง canonical state ใหม่ |
| E-180 | 2026-08-27 | self-contained battle gate path | census state หลักฐานใน workspace พบ `build/repro/en-th-own-native11.mss` เป็น state เดียวที่ replay current production ตรง golden 6/6; ตั้งเป็น default ของ `check_proven_runtime.py` และแก้ README ให้รันโดยไม่ต้องส่ง path ภายนอก | battle regression รันเองจาก workspace ได้จริง; command/status gates ยังต้องสร้าง canonical state ใหม่แทน alias ที่หายไป |
| E-181 | 2026-08-27 | current main/system runtime gate | ใช้ canonical state เปิด map menu แล้ว genuine redraw 3 branch บน production เดียวกัน: UNITS แสดงรายชื่อ/ชื่อไทย, ORDER แสดงคำสั่งและรายชื่อไทย, SYSTEM แสดงกริด/เสียง/ปุ่มลัดและปุ่มไทยครบ. เพิ่ม `check_main_menu_runtime.py` พร้อม default state และ golden hash สามภาพ | `menu.main_and_system` ย้ายเป็น runtime_verified และ rerun จาก workspace ได้โดยไม่ต้องรับ state เพิ่ม |
| E-182 | 2026-08-27 | canonical command/status state | สร้าง `build/repro/current-command-canonical.mss` ผ่าน native route canonical battle → map menu → UNITS → เลือกยูนิต → command menu; hash state `fb9036c4…ec7ee`. เปลี่ยน command/status checkers ให้ใช้ state นี้เป็น default และไม่กด A ซ้ำตอนเริ่ม; golden ปัจจุบันยืนยัน open/down/wrap/reentry/Spirit และ UNIT/PILOT/WEAPON พร้อมภาพสะอาด | command และ status gates ไม่พึ่ง alias state ของผู้ใช้แล้ว และรันเองจาก workspace ได้; Attack ยังไม่ปรากฏเพราะยูนิตนี้ไม่มีศัตรูในระยะ |
| E-183 | 2026-08-27 | post-AI Attack census | SUPERSEDED โดย E-185: state/census อ่าน records stale และบาง state อยู่ใน post-move Wait menu ไม่ใช่ map-ready state | ถอนข้อสรุป 143 ตำแหน่งทั้งหมด |
| E-184 | 2026-08-27 | forward-unit Attack attempt | ภาพยืนยัน Move ขึ้นซ้าย 3+3, Wait, enemy turn และ battle จริง; อ่าน HUD ใหม่พบศัตรู HP 800 ถูกทำลาย ส่วนยูนิตฝ่ายเรา HP 3200 รอด. ส่วน census ท้ายรายการ SUPERSEDED โดย E-185 | route gameplay ยังเป็นหลักฐานได้ แต่ผล census/ข้อสรุปว่ายูนิตฝ่ายเราถูกทำลายถูกถอน |
| E-185 | 2026-08-27 | Attack census correctness repair | พบ savestate คง `$7E:CED6=C7A5`, count และ record list จากเมนูก่อนหน้า; probe เดิมจึง false-positive แม้ภาพอยู่ map. เพิ่ม clear marker ก่อน input และก่อน A สุดท้าย, รายงาน records เฉพาะ cookie ที่ถูก claim ใหม่ และขยาย input pulse เป็น 6 polls/spacing 12 เพื่อผ่าน field parity และ map cursor polling. ถอน auto-B เพราะ post-move B ยกเลิกการย้ายและเปลี่ยน gameplay state | census ใหม่ต้องเริ่มจาก map-ready state และผลเก่าทั้งหมด E-174/E-183/E-184 ส่วน census ห้ามใช้ |
| E-186 | 2026-08-27 | map-ready state audit | visual probes พิสูจน์ว่า state ที่ตั้งชื่อ `current-forward-map*.mss` ยังสลับอยู่ระหว่าง movement/post-move Wait: B ย้อน move, A ที่ hold ข้าม transition และ cursor+A ย้ายยูนิตไปปลายทางว่าง. ผล census จาก state เหล่านี้จึงไม่รับเป็น Attack evidence แม้ probe core แก้ stale marker แล้ว | ต้องสร้าง state ใหม่ด้วยการเซฟจาก emulator หลังเห็น map idle และไม่มี selected unit หรือใช้ save ที่ลูกพี่ส่งมา; ห้ามตีความภาพ map เพียงเฟรมเดียวว่า map-ready |
| E-187 | 2026-08-27 | tracker reconciliation | ตรวจ PLAN, surface inventory และ E-101–E-186 ใหม่ แล้วปรับ phase/feature/blocker ที่ยังค้างข้อความตั้งต้น: P5/P6 เป็น done ตาม runtime/catalog evidence, P8/P9 ยัง in progress, inventory 36 surfaces มี unknown 0 แต่ pending 8/runtime partial 2 ณ checkpoint นี้ | เอกสารไม่อ้างว่า architecture ส่วนที่พิสูจน์แล้วเป็น TODO และไม่ยกระดับ remaining surfaces เป็น runtime verified ก่อนมี genuine redraw |
| E-188 | 2026-08-27 | isolated cold boot naming/intro | ใช้ ROM ชื่อใหม่ใน `/private/tmp` เพื่อไม่รับ battery save เดิม; production SHA `aebbc8e…90d38` cold boot ถึง naming frame 900 แสดง labels และ preset ไทยสะอาด, confirm frame 1500 แล้วเข้า intro ไทย frame 2000–3200. เอฟเฟกต์คลื่น/ช่องขาว frame 1800–1900 เกิดเหมือน clean ROM control ทุกขั้น | ปิด naming labels/presets และยืนยันว่า transition ที่ดูคล้าย tile แตกเป็น stock effect; keyboard/typed buffer/save-load ยังไม่ถูกทดสอบ |
| E-189 | 2026-08-27 | cold-transition diagnostic harness | เพิ่ม `cold-naming-settle.lua` และเก็บ current/clean control ทุก 100 เฟรมถึง 3200 ทำให้แยก transient stock transition ออกจาก corruption ได้โดยไม่เดาจากเฟรมเดียว | ใช้ harness นี้เป็น regression ของ naming-confirm → intro; ไม่ใช้ frame 1800/1900 เป็น visual golden |
| E-190 | 2026-08-27 | isolated title/menu redraw | เพิ่ม no-input splash observer และ one-pulse title script; isolated current ROM วาด logo/title transition แล้วหยุดนิ่งที่ START/LOAD/CONTINUE/OPTION frame 900 โดยไม่เข้า naming หรืออาศัย savestate | `title.menu` เป็น runtime_verified; Japanese logo ยังคงอยู่ตาม exception policy |
| E-191 | 2026-08-27 | battle frame-900 boundary audit | regression หลัง cold boot ได้ hash ใหม่ `78bf80ea…5596` ที่ frame 900; probe frames 899/900/901 พิสูจน์ว่าเป็นสอง safe adjacent phases: quote ไทยเฟรมสุดท้ายและ battle frame แรกหลัง clear โดยภาพ/tiles ไม่แตก. เพิ่มทั้งสอง hash ใน golden เหมือน boundary 1100/1600 ที่ประกาศไว้แล้ว | gate ไม่ false-fail ตาม input-poll parity และยังปฏิเสธภาพอื่นทั้งหมด; ไม่เปลี่ยน production ROM |
| E-192 | 2026-08-27 | cold input harness correction | พบสคริปต์ cold/route หลายตัวเรียก `emu.setInput` เฉพาะตอนมีปุ่ม ทำให้ปุ่ม pulse ล่าสุดอาจค้าง; แก้ `intro-cold-start`, `cold-naming-settle`, `cold-title-menu`, `cold-naming-input`, `route-shots` และ `native-save-map-check` ให้ส่ง `{}` ทุก input poll. Rerun title ได้ hash เดิม และ corrected naming-confirm → intro frames 2000/2400/3200 ตรงหลักฐานเดิม | interaction evidence ต่อจากนี้ควบคุม press/release จริง; keyboard ไทยอยู่หน้า intermission name-change ไม่ใช่ protagonist preset screen จึงห้ามอ้าง cold setup ว่าทดสอบ keyboard |
| E-193 | 2026-08-27 | automated isolated cold gate | เพิ่ม `check_cold_runtime.py`: copy ROM เป็นชื่อชั่วคราวแยก title/flow, รัน Mesen โดยไม่มี battery save เดิม, ตรวจ title frame 900, static naming crop frame 900 และ intro frames 2000/2400/3200. Static crop จงใจไม่รวม face/preset ที่เกมสุ่ม; gate ผ่านบน SHA `aebbc8e…90d38` | title, naming labels/presets/confirm และ intro rerun ได้จาก workspace; keyboard/type/delete/save-load ยังต้อง intermission SRAM/state |
| E-194 | 2026-08-27 | production battle WRAM isolation gate | ขยาย `check_proven_runtime.py` ให้ตรวจ full-WRAM dump ทุก golden frame: ordinary state `$7E:FFA0–FFBF` ต้องคงเดิมตลอด battle, battle state `$7E:FFC0–FFDF` และ scratch `$7E:FFE0–FFFF` ต้องเปลี่ยนอย่างน้อย 3 lifecycle phases และต้อง settle ตรงกันที่ frame 1600/2400 ก่อน map return; dump ต้องครบ 128 KB ทุกเฟรม | battle renderer ไม่เขียนข้าม ordinary block และ private state/scratch คืนสภาพนิ่งหลัง lifecycle; full DMA/VBlank timing ยังแยกเป็น P9 blocker |
| E-195 | 2026-08-27 | canonical battle DMA regression budget | พบ DMA probes เดิมอ่าน `$420B` ภายใน write callback จึงได้ค่าก่อน write/phantom channel; แก้ `wram-snapshots`, `intro-dma` และ `p1-command-probe` ให้ใช้ callback `value`. Correct trace 2,450 เฟรมมี contract ch0→OAM `$04` 544 bytes, ch1→CGRAM `$22` 512 bytes และ ch2→VRAM `$18`; peak รวมต่อเฟรม 32,768 bytes. ฝัง allowed contract/length และ ceiling `$8000` ใน battle gate | ป้องกัน DMA channel/length/peak regression บน route นี้; ceiling เป็น measured regression bound ไม่ใช่หลักฐานว่า transfer อยู่ใน VBlank ทุกครั้ง จึงยังไม่ปิด scanline timing |
| E-196 | 2026-08-27 | canonical DMA VBlank/forced-blank timing | sample `$4212` (HVBJOY) และ tracked `$2100` (INIDISP) ณ write `$420B`: 1,934/1,938 transfers อยู่ใน VBlank; อีก 4 VRAM transfers เป็น scene-transition blocks frame 561/1699 และมี forced blank `$2100=8F/80`. เพิ่ม gate ให้ทุก DMA ต้องมี VBlank bit หรือ forced-blank bit มิฉะนั้น fail | canonical battle/animation/map-return route ไม่มี DMA ตอนจอ active นอก VBlank; remaining surface routes ยังต้องผ่าน gate ของตนก่อนปิด P9 ทั้งหมด |
| E-197 | 2026-08-27 | command labels ไทยจากต้นฉบับญี่ปุ่น | เปลี่ยน visible P7 records เป็น `เคลื่อนที่`, `โจมตี`, `ซ่อม`, `แปลงร่าง`, `รวมร่าง`, `แยกร่าง`, `พลังจิต`, `เติมเสบียง`, `ใต้ดิน`, `ใต้น้ำ`, `พื้นดิน`, `ขึ้นบิน`, `สถานะ`, `รอ`, `ชิ้นส่วน`; เลือก `สถานะ` แทน `ความสามารถ` เพราะกรอบจริงรองรับ 56px. Build current SHA `1e21408f…cdf25b7`, `tests/test_rom.py` 20 ผ่าน และ Mesen command gate 5/5 ผ่าน; ภาพ reentry ตรวจด้วยตาแล้วไม่มี clipping/glyph แตก | labels ไทย current-source ผ่าน canonical command route; P7 ยังไม่ปิดจนกว่าจะมี Attack state |
| E-198 | 2026-08-27 | Thai main-title logo | แทนโลโก้ญี่ปุ่นด้วย `ซูเปอร์โรบอตวอร์ส 4` จาก concept ที่อนุมัติ แปลงเข้า stock OBJ 16×16 จำนวน 53 ชิ้นและ palette เดิม 16 สีโดยไม่แตะ Earth background/English menu; isolated cold frame 900 hash `7a2127cb…b51734`, ROM SHA `1e1b36db…ea1db`, title tests 6 ผ่าน | title logo/menu วาดจริงจาก cold boot; legal credits ยังคงต้นฉบับ |
| E-199 | 2026-08-27 | connected title-logo extrusion | สร้างเงาใหม่จาก face mask แบบต่อเนื่องลึก 14px แทนการย่อเงาจาก concept ตรง ๆ; ไล่ stock blue palette `$4C00/$3C00/$2C00/$1C00` ใกล้→ไกล ทำให้เงาติดขอบอักษรและไม่ขาดช่วง. Cold frame 900 hash `ed97d1b3…c5ae35`, ROM SHA `baa57552…03c19` | supersede raster ของ E-198; ข้อความ/ฉากโลก/เมนูไม่เปลี่ยน |
| E-200 | 2026-08-27 | permanent command-surface lifecycle repair | แก้ overlay record header เป็น `+7/+8`, flush blank tail ครบ 6 cells, allocate tile rows จาก record index, suppress legacy frame stores แบบ cookie+geometry guard และให้ selection wrapper เรียก stock parser เพื่อรักษา tilemap-upload request ก่อน mirror palette band. `save/cmdmenu.mss` ผ่านกรอบ 8 ช่อง/5 rows, Down/Up-wrap/reentry/Status; `cmdmenu-broken.mss` ฟื้นสะอาดหลัง close/reopen; จับเฟรม 18–70 พบ map เดิมถึง frame 60 แล้ว surface สมบูรณ์ที่ frame 61 โดยไม่มี transient frame. Command 5/5, Status 3/3, battle 6/6, pytest 244 passed; deterministic ROM SHA `a9f02f68…864fe` | ไม่ใช้ global NMI/PPU hook และไม่ต้องขยาย ROM เพิ่ม; savestate เก่าที่ค้าง VRAM ต้องผ่าน lifecycle close/reopen หนึ่งครั้ง แล้ว ROM จะ redraw/ล้าง footprint ตาม contract |

| E-201 | 2026-08-27 | hand-tuned Thai title-logo source | เพิ่ม direct pixel editor ที่บันทึก atomic ลง `data/assets/title-logo.json`; ลูกพี่เกลาพิกเซลจริงครบ palette `0–F`, manual pixel SHA `59353756…11ce2b`. Production build อ่านไฟล์นี้โดยตรง, importer ปฏิเสธการทับถ้าไม่มี `--force`. Mesen cold frame 900 hash `c1897dbd…f350553`, title/editor/current tests 9 ผ่าน, deterministic full ROM SHA `55d35f44…b0e04` | supersede auto-mask E-198/E-199; bitmap งานมือเป็น source of truth |

| E-202 | 2026-08-27 | revised hand-tuned title logo | รับ manual bitmap รอบสองจาก editor, pixel SHA `2658839b…2c5bb`; geometry `200×64`, palette `0–F`, title/current tests 9 ผ่าน. Mesen cold frame 900 hash `51a8f5c8…3d1717`, deterministic full ROM SHA `f1e46df5…92cf4a` | production ROM และ golden ใช้งานงานเกลารอบล่าสุดแล้ว |

| E-203 | 2026-08-27 | command first-visible footer repair | save state `save/cmdmenu.mss` ชี้ว่า stock repeated-word writer สร้าง footer 3 tiles ก่อน cleanup DMA; resolve catalog `$0022` ผ่าน stock table `$C9:00D8` ได้ record `$CC:966E` และเปลี่ยนเฉพาะ `F2 03 1A20` ที่ PC `$0C969C` เป็น blank tile `F2 03 1000` โดยคงจำนวน store/timing เดิมและไม่ hook hot path `$81:8B39`. ROM เดิม fail first-visible hash `f6788475…fdc6`; ROM ใหม่ได้ stable hash `0451f0db…a10c` ตั้งแต่เฟรมแรก. Mesen command 6/6, status 3/3, main menu 3/3, battle 6/6 และ pytest 243 ผ่าน; SHA `13d4d9e6…03c6` | แก้เฉพาะข้ออ้าง “ไม่มี transient frame” ของ E-200 ซึ่งเก็บภาพช้ากว่า first-visible; เพิ่ม frame 107 เป็น permanent command regression และไม่เปลี่ยน timing ของ surface อื่น |
| E-206 | 2026-08-27 | en-timed command publish | เทียบ `save/cmdmenu.mss` กับ `save/cmd_en.mss`: ไทยเดิมแสดงที่ frame 106 หลัง A frame 80 ส่วน en frame 91. Trace พบไทยเขียน shadow tilemap 8,458 ครั้งเทียบ en 196 เพราะ full redraw ทุก glyph. เปลี่ยน overlay เป็น planar 4bpp สำเร็จรูป, copy แบบ 32-byte unrolled blocks, redirect stock writer ไป y=29–30 นอก viewport และ publish frame/content ครั้งเดียวหลัง parser. Canonical first-visible ขยับ 107→88; เมนู 3/4 rows ไม่มีกรอบ/tile transient และ highlight 4 rows เปลี่ยนเป็น stable state เดียวหลัง input 2 frames. Command 6/6, status 3/3, main 3/3, battle 6/6 และ pytest 243 ผ่าน; SHA `58c27bd0…ff99a5` | lifecycle ใกล้ en โดยไม่ยืม patch/runtime; overlay ขยายเป็น 5,882 bytes และย้าย route/index ไป `$FC:7000/$FC:7100` โดย allocator ตรวจพื้นที่ว่าง |
| E-207 | 2026-08-27 | EN single-owner dynamic command frame | `save/cmdmenu-broken.mss` พิสูจน์ว่า E-206 ยังซ้อนกรอบ custom คงที่กับเศษกรอบ stock ที่ย้ายตามตำแหน่งยูนิต (`x14–16,y5–7`). เปลี่ยน catalog `$0022` ตาม EN จริง: repeat width ทั้ง 4 จุด `F2 03→F2 06` และ cursor adjust `FC→F9`; ถอน hooks ที่ suppress frame stock, ค้น top edge 8 tiles หลัง parser แล้ว map Thai overlay ลงกรอบ stock ชุดเดียว. State เดิมที่พัง, canonical และ route 4 rows เปิด/Down/Up-wrap/reentry สะอาดตั้งแต่ first-visible; command gate เพิ่ม dynamic-position 2 cases, status/main/battle และ pytest 243 ผ่าน; SHA `248259c0…7b5c5` | กรอบ command เป็น dynamic stock-owned surface แบบ EN; ไม่มี fixed second frame หรือ fixed-coordinate cleanup อีก |
| E-208 | 2026-08-27 | dynamic border completion | ภาพจริงเผยว่า E-207 ตรวจย่อจนพลาดขอบซ้าย/ขวา/ล่าง: การ redirect `$18` ทำให้ stock เดิน cursor จาก hidden row หลังแถวแรก และเมื่อเลิก redirect จำนวน token ไทยที่ไม่มี EN padding ทำให้แถวล่างเลื่อนไปซ้าย. เปลี่ยนเป็น suppress เฉพาะหก glyph stores โดยคง cursor stock, ใช้ top edge ของ stock หา anchor dynamic แล้วล้าง footprint จาก anchor ก่อน `draw_window_frame` วาดสี่เหลี่ยม 8×N ครบ. Tilemap gate ตรวจทุกด้านโดยตรง: `2011 + 6×2019 + 2012`, ทุกแถว `201B/201C`, ล่าง `2013 + 6×201A + 2014`; state broken/canonical/4-row และ highlight สะอาด; SHA `c291ac4b…6620c` | E-207 superseded เฉพาะข้ออ้างว่ากรอบ stock สมบูรณ์เอง; geometry/anchor ยังตาม EN แต่ Thai proportional stream ต้อง finalise frame จาก anchor |
| E-209 | 2026-08-28 | command border halo cleanup | `save/cmdmenu.mss` ยังเหลือมุมล่างซ้าย `2013` หนึ่งก้อนที่ `x11,y12` นอกกรอบจริงซึ่งเริ่ม `x13`; route 4-row เดิมยังเคยเหลือ `201C` ขวาหนึ่งช่อง. ขยาย cleanup จากหนึ่งช่องซ้ายเป็น dynamic margin สองช่องทั้งซ้าย/ขวาโดย clamp ที่ขอบ 32-cell row. Command gate ตรวจเพิ่มว่าพื้นที่สองช่องรอบกรอบเป็น blank `0010` ทุกแถว จึงจับ tile ยื่นแม้กรอบหลักครบ; open/first-visible/down/wrap/reentry/dynamic-position ผ่าน; SHA `e4cf9e17…6fcedd` | แก้เฉพาะ halo รอบ command frame ไม่ขยายพื้นที่ล้างไปยัง surface อื่น |
| E-210 | 2026-08-28 | EN-owned command tilemap lifecycle | `cmdmenu-broken.mss` มี CPU `$00:F44C`/SP `$001D` ยืนยัน crash จาก custom lifecycle. เปลี่ยน command records เป็น stream 6 direct cells, ให้ stock writer วาด tilemap/กรอบ/highlightทั้งหมด และให้ Thai raster เติม bitmap ทีละ 8×16 cell โดยตั้ง `$D0` เฉพาะก่อน store แล้วคืนปลายแถวตาม DMA contract. ถอน hooks glyph-store 6 จุด, custom selection และ post-parser frame redraw. Replay `cmdmenu.mss` ผ่าน first-visible/open/down/up-wrap/reentry/Spirit; เดิน Right×2 เปิดยูนิต 4 rows ผ่าน open/down/wrap/reentryโดย CPU/SP ปกติ. Command/status/main/battle gates และ pytest 244 ผ่าน; SHA `c5717c00…9460e44b` | command menu ใช้ ownership แบบ EN จริง; Thai renderer เปลี่ยนเฉพาะ pixels/arena ไม่ยึด tilemap lifecycle |
| E-211 | 2026-08-28 | six-cell command highlight | stock selection เปลี่ยน palette `$0400` เพียง 3 cells แม้ geometry/ข้อความเป็น 6 cells. เพิ่ม mirror ที่เปลี่ยนเฉพาะ bit นี้: initial raster หา exact tile ids `$0100…010A`; navigation รอ stock parser และยืนยันกรอบ dynamic ก่อน sync ทั้งแถว. `save/menu.mss` ผ่าน open/down/up-wrap/reentry/sweep 4 rows โดย selected=`$25xx×6`, normal=`$21xx×6`; status 3/3, main 3/3, battle 6/6 และ pytest 245 ผ่าน; SHA `c440492f…f356f308` | ไม่เปลี่ยน tile id, border หรือ renderer pixels; highlight ครอบความกว้างไทยเต็ม 6 cells |
| E-212 | 2026-08-28 | unit shield ghost-tail repair | `save/shield.mss` ทำซ้ำอาการได้ว่า byte วรรณยุกต์ท้าย `ล่` ทำให้ stock compositor เตรียม tilemap เกินอีกช่องและเหลือรูปคล้าย `S` หลัง `ไม่มีโล่`. คืน shorthand slot จาก `ปิ` ให้ phrase `โล่` ขยาย `โ+ล+่` ใน renderer call เดียว; payload เหลือ `40 B3 A3 CB FF` แต่ความกว้างคง 33px. Replay `B→A` ได้ screenshot hash `13c7f208…ade4da`; static font tests และ deterministic build ผ่าน | unit-ability gate ใช้ state โล่โดยตรงแล้ว; ห้ามแยกวรรณยุกต์ท้ายกลับเป็น source byte เดี่ยว |
| E-213 | 2026-08-28 | command/status shared-bank recovery | พบหลังเข้า `สถานะ` ว่า stock นำ source slot แถวคำสั่งสุดท้ายกลับไปใช้กับ record `$D2:88C8` (`NEWTYPE`) และ pointer `$FA:0464`; เดิม adapter เห็นแค่ bank `$FA` จึงรับ stream หน้าสถานะมาทับแถวคำสั่ง. จำกัด raster/cell hook ให้รับเฉพาะช่วง private command streams และเปิด cached-row recovery เฉพาะระหว่าง parser catalog `$0022`. Stress `สถานะ→ออก→เปิด→เปลี่ยนร่าง` สองรอบได้กรอบ 8×10, 4 แถว และ highlight 6 cells สะอาด; focused tests 26 ผ่าน; SHA `558acfad…752fcc9b` | แก้ lifecycle ที่บางตัวละครพังตาม dynamic ability record โดยไม่ยึด Transform submenu หรือ stream `$FA` ของหน้าจออื่น |
| E-214 | 2026-08-30 | complete Thai title-logo extrusion | สร้างเงา stock-blue `C–F` ใหม่จาก face งานมือเดิมแบบ solid sweep ลึก 14px เอียงขวา 1px ต่อความลึก 3px; face เปลี่ยน 0 พิกเซลและ shadow 4,447 พิกเซล. EN tile mapping round-trip ตรงทุกพิกเซล, title/editor/shadow tests 7 ผ่าน, deterministic ROM SHA `7c8907cf…c302066`. Mesen cold redraw เฟรม 900/1200/1800 คงที่ hash `93995727…3972a` และไม่แตะ Earth background/English menu | `data/assets/title-logo.json` เป็น source-of-truth; `tools/rebuild_title_logo_shadow.py` ล็อก geometry/palette และป้องกันเงาขาดกลับมา |
| E-215 | 2026-08-30 | title-logo shadow weight refinement | ลด solid extrusion ตามคำสั่งจาก 14px เหลือ 10px โดยคงทิศเอียงและ palette `C–F`; face เปลี่ยน 0 พิกเซลและ shadow เหลือ 3,660 พิกเซล. Title/editor/shadow tests 7 ผ่าน, deterministic EN ROM SHA `946ed2f9…8a26a70`. Mesen cold redraw เฟรม 900/1200/1500/1800 คงที่ hash `da682c7c…462702`; ภาพจริงยังต่อเนื่องแต่ไม่บดน้ำหนักหน้าตัวอักษร | supersede เฉพาะ depth 14px ของ E-214; source-of-truth และ regression contract เหมือนเดิมแต่ล็อก depth 10px |
| E-216 | 2026-08-30 | Thai Field Command terminology | ล็อกคำสั่ง 15 รายการเป็นไทยเต็มและเปลี่ยน `Spirit` จาก `พลังจิต` เป็น `สปิริต`; overlay วัดได้ 27px จากเพดาน 48px และคำกว้างสุด 47px. แทนช่องคีย์บอร์ดของ ฃ/ฅ/ฦ ที่ถอดแล้วด้วยเครื่องหมายไทยใช้จริงให้ contract กลับมาครบ 63 ช่อง. Full EN ROM build สองรอบ byte-identical SHA `374189c…c56956` | static/build ผ่าน; ต้อง rerun command-menu genuine redraw กับ state ฝั่ง EN ก่อนนับ runtime verified |
| E-217 | 2026-08-30 | Spirit selector VRAM overflow repair | `save/bug.mss` ยืนยัน Spirit-name VWF ใช้ guard 3 tile-pairs ต่อ precomposed cluster จน `$D0=$01B4` และอัปโหลด 13,952 bytes ทับ terrain VRAM. เพิ่ม `compact_grid` เฉพาะ renderer นี้ให้ใช้ 1 pair ต่อ clusterโดยคง source/page/tilemap เดิม; genuine redraw จาก `save/field.mss` แสดงชื่อครบและปิดกลับแผนที่สะอาด, `$D0=$014C`/help `$0164`, list upload 10,624 bytes ต่ำกว่า EN reference `$0174`. Tests ที่เกี่ยวข้องผ่าน 60 รายการ (มี golden icons drift เดิม 1 รายการ); deterministic EN build SHA `3d1d96b4…a65a62` | จำกัด compact allocation เฉพาะ ordinary cell-cursor Spirit grid; surface อื่นยังใช้ guard contract เดิม |

### หลักฐานที่ต้องเพิ่มใน P1

- E-101: clean/en byte-diff classification
- E-102: en menu entry/exit trace
- E-103: source stream/pointer ของ labels
- E-104: tilemap/frame dump
- E-105: VRAM/DMA/glyph upload dump
- E-106: geometry table และ screenshot genuine redraw

---

## 6. Decision log

| ID | วันที่ | การตัดสินใจ | เหตุผล | ผลกระทบ |
|---|---|---|---|---|
| D-001 | 2026-08-24 | PLAN.md ใหม่แทน plan รุ่นเก่า | เป้าหมายเปลี่ยนจากรักษา 3 cell เป็นเทียบคุณภาพ en | ต้อง recheck renderer/window/catalog |
| D-002 | 2026-08-24 | precompose cluster ที่เกมใช้จริงเป็นหลัก | ลด runtime combining bug และทำให้ metrics deterministic | ต้อง generate atlas จาก corpus |
| D-003 | 2026-08-24 | English production ใช้ new glyph 100% | ไม่ให้มี stock/new renderer ปนกันกลางบรรทัด | ต้องครอบคลุม digits/control ที่ runtime inject |
| D-004 | 2026-08-24 | VWF วัดจาก pixel metrics | รองรับ Thai/English และชื่อยาวอย่างถูกต้อง | window ต้องวัดก่อนวาด |
| D-005 | 2026-08-24 | string pool variable-length | ขจัดข้อจำกัด byte/record แบบเดิม | ต้อง repoint table และเพิ่ม overflow report |
| D-006 | 2026-08-24 | window/tilemap ขยายตาม surface policy | กรอบเดิม 3 cell ไม่ใช่ target | ต้องแก้ frame, list, cursor และ VRAM budget |
| D-007 | 2026-08-24 | en ใช้เป็น reference ไม่ใช่ source ของ build | แยก behavior ที่พิสูจน์แล้วจาก byte ที่บังเอิญเหมือนกัน | ทุกข้อสรุปต้องมี trace/geometry evidence |
| D-008 | 2026-08-24 | เก็บ legacy evidence ที่ยังมีประโยชน์ | ป้องกันการเสียข้อมูล reverse-engineering | archive ไม่ใช่ normative spec |
| D-009 | 2026-08-25 | extended token ใช้ 4 pages ตาม manifest | ทำให้ encoder, decoder และ renderer ไม่มี owner conflict กับ engine `$F4/$F5` | byte bands ถูก lock และต้อง bump version หากเปลี่ยน |
| D-010 | 2026-08-25 | `<Gap>`/`<Pad>` เป็น typed layout control ในอนาคต ไม่ใช่ glyph | `<Pad>` ไม่มี pixel/advance และ `<Gap>` เป็น spacing policy; ทั้งคู่ไม่ควรกิน atlas slot โดยปริยาย | current production corpus ห้ามใช้จนกว่าจะมี control schema |

---

## 7. Open blockers และความเสี่ยง

| ID | สถานะ | เรื่อง | ผลกระทบ | วิธีปลด |
|---|---|---|---|---|
| B-001 | CLOSED | active en menu path | E-101–E-107 trace parser/source/tilemap/frame/cursor/DMA ครบ | รักษา artifacts เป็น P1 evidence |
| B-002 | OPEN | runtime provenance ของ remaining surfaces ยังไม่ครบ | อาจมี stock glyph หรือ redraw defect ในจอที่ยังไม่เปิดจริง | ปิด pending/runtime_partial inventory ทีละ route |
| B-003 | CLOSED | runtime numbers/controls contract | E-201–E-205 และ runtime gates ครอบคลุม typed controls/numeric paths ที่เปิดใช้แล้ว | เพิ่ม fixture เมื่อพบ control class ใหม่ |
| B-004 | PARTIAL | canonical battle/map route ผ่าน DMA ceiling และ VBlank/forced-blank timing | remaining naming/intermission/option/part routes ยังไม่มี timing evidence | ใช้ timing gate เดียวกันเมื่อมี state ของแต่ละ surface |
| B-005 | OPEN | intermission name-change keyboard/typed buffer/save lifecycle ยังไม่มี gate | cold labels/preset/confirm ผ่านแล้ว แต่ keyboard ไม่ได้อยู่ใน protagonist setup; การพิมพ์/ลบ/save-load อาจยังผิด | ใช้ isolated copy ของ intermission SRAM/state แล้วทดสอบ type/delete/confirm/save/load |
| B-007 | OPEN | command Attack branch ไม่มี map-idle state ที่ศัตรูอยู่ในระยะ | ปิด P7 behavior gate ไม่ได้ | สร้าง native map-idle savestate แล้ว replay repaired census |
| B-008 | OPEN | intermission/option/part-effects ไม่มี same-build runtime state | ปิด P8/P9 redraw inventory ไม่ได้ | เก็บ state ตอน intermission และเปิดแต่ละหน้าจอ |
| B-006 | CLOSED | current docs/config บางส่วนเป็น snapshot ของ architecture เก่า | ใส่ legacy/reference label และแก้ link แล้ว | รักษา PLAN/PROGRESS เป็น source of truth |

P0–P6 ทำเสร็จแล้ว; blocker ปัจจุบันจำกัดเฉพาะ runtime routes ใน P7–P9

---

## 8. Session log

### 2026-08-24 — Replan และ documentation cleanup

ทำแล้ว:

- ตรวจรายการ docs, source, tools, tests และ git baseline
- พบข้อสรุปเก่าว่า unit command menu ต้องอยู่ 3 cell ซึ่งขัดกับภาพ en
- กำหนด architecture ใหม่: new glyph + precompose + VWF + variable pool + dynamic window
- ลบเอกสาร plan/test Renewal ที่ superseded แล้ว
- เขียน PLAN.md ใหม่
- สร้าง tracker นี้

ผลตรวจ cleanup:

- อัปเดต header/reference ของ docs/00, docs/03, docs/04, docs/05, docs/06, docs/99 และ README แล้ว
- ตรวจ broken links, JSON syntax และ whitespace หลัง cleanup แล้ว
- baseline build/report รันใหม่แล้ว: `.venv/bin/python tools/build.py --check` ผ่าน deterministic

---

## 9. งานถัดไปตามลำดับ

1. สร้าง isolated copy ของ intermission `f-naming.srm` แล้วตรวจ keyboard/type/delete/confirm/save-load
2. หา native map-idle command state ที่มี Attack ในระยะและปิด P7 branch gate
3. เก็บ same-build intermission/option/part-effect states และปิด redraw inventory
4. วัด VRAM/DMA/VBlank/WRAM guard ใน full route แล้วทำ P10 release audit

ห้ามเปิด generic story blitter ใน battle อีกจน adapter เฉพาะทางผ่าน route ครบ lifecycle
